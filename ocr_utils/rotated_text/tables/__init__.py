"""Таблицы с повёрнутым текстом: найти боковые ячейки, прочитать, перерисовать прямо.

Цепочка на одну таблицу (``pipeline``):

1. ``source`` — рамка таблицы из базы разметки и вырезка из заострённой копии полосы;
2. ``structure`` — сетка ячеек по линейкам (детектор из ``page_layout.tables``);
3. ``orientation`` — какие ячейки повёрнуты и в какую сторону;
4. ``ocr`` и ``second_opinion`` — чтение боковых ячеек (tesseract, при сомнении — surya);
5. ``fit`` и ``dpi`` — каким кеглем текст влезает в ячейку и какой DPI для этого нужен;
6. ``reshape`` — если DPI не хватает: поворот всей таблицы или расширение колонок;
7. ``render`` — закраска старого текста и набор нового;
8. ``sheets`` — листы «было-стало» для проверки глазами.

Вписывание получившейся таблицы обратно в страницу здесь не делается: результат —
картинка таблицы, её новая сетка и требуемый DPI страницы.
"""
