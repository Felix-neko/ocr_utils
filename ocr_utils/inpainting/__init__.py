"""Закрас областей по маске — общий для пальцев и для разметки из CVAT.

Пакет собран из того, что раньше лежало внутри ``scan_cropping.finger_removal`` и
умело обслуживать только пальцы. Здесь остался механизм, а вся предметная часть
(откуда берётся маска, какими словами описывать фон для Stable Diffusion) — у
потребителей: ``scan_cropping.finger_removal`` и ``scan_cleanup``.

Устройство:

* :mod:`roi` — геометрия ROI вокруг зоны и растушёванная вклейка результата;
* :mod:`grouping` — во что складывать связные области: по одной или группами;
* :mod:`apply` — цикл «маска → группы → ROI → заливка → вклейка»;
* :mod:`backends` — заливщики ROI (LaMa, Stable Diffusion) за одним интерфейсом.

Сети здесь не живут: их единственный владелец — ``scan_cropping.gpu_models``,
чтобы видеопамятью в процессе распоряжался ровно один объект.
"""
