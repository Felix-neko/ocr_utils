"""Помойка: неподдерживаемый код и заброшенные эксперименты.

Сюда складывается всё, что больше не участвует в рабочем пайплайне
(``ocr_utils.scan_cropping``), но выбрасывать жалко. Код здесь поддерживается
ровно настолько, чтобы ИМПОРТИРОВАТЬСЯ (пути импортов при переезде починены);
тестами он не покрыт, на актуальность зависимостей и корректность результатов
никто не проверяет. Опись содержимого — в ``README.md`` рядом.

Правило: из рабочего кода сюда не импортируются. Зависимость наоборот
(legacy тянет что-то из ``scan_cropping``) допустима.
"""
