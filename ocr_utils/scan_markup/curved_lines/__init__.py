"""Поиск полос с кривыми, волнистыми или неравномерно наклонёнными строками.

ЗАЧЕМ. FineReader с включённой коррекцией геометрии (искажение строк, перекос, трапеция)
выправляет полосы, у которых строки реально изогнуты — прогиб у корешка, «волны», блок
текста, повёрнутый относительно остальной полосы, — и портит полосы, у которых строки
прямые. План: два пакетных прогона FineReader, с коррекцией и без, и сборка итогового PDF
постранично. Для этого нужно знать, на каких полосах строки кривые.

Подпакет ничего не исправляет: он МЕРЯЕТ. Несколько поддетекторов независимо оценивают
одну полосу, каждый отдаёт свои непрерывные метрики (все в шкале «больше — кривее»), по
порогам ставится флаг, и находки раскладываются симлинками — подкаталог на детектор плюс
сводный ``combo/``. Пороги калибруются по CSV, где лежат все метрики всех полос.

Кривизна бывает трёх видов, и ловят их разные детекторы:

* прогиб у корешка — кривизна отдельных строк и «рваный» край колонки (``line_fit``,
  ``surya_lines``);
* волны по всей полосе — наклон меняется и вдоль строки, и от строки к строке (все);
* локальный поворот блока — строки прямые, но угол верхнего блока не совпадает с углом
  нижнего; кривизны нет, есть РАЗБРОС локальных углов (``skew_map``, ``strip_shift``).
"""

# Версия набора детекторов. Входит в ключ дискового кэша измерений вместе с версией каждого
# детектора: поднимать при изменении общего контракта (что лежит в ``Measure``, как считается
# score), а не при правке отдельного алгоритма — у того своя ``Detector.version``.
CURVED_LINES_VERSION = 1
