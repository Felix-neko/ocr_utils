# Пользовательское сообщение — перевод `user.md.j2` и фразы по умолчанию из `__init__.py`

В запрос уходит английский оригинал; это перевод для чтения. Ветки шаблона — `[если …]`.

---

`[если тайлов больше одного:]`
Страница дана как {{ntiles}} перекрывающихся тайлов: {{ncols}} столбец(ов) × {{nrows}} строк(и)
страницы, перечисленных по столбцам, каждый столбец сверху вниз `[если столбцов больше одного:]`,
весь левый столбец раньше правого. Соседние тайлы слегка перекрываются. Считай их ОДНОЙ
страницей: перепиши её один раз, в порядке чтения, не повторяя строки в зоне перекрытия.

`[если это второй проход (подсказка собрана из ответа первого):]`
Первое чтение этой страницы сообщило о повреждении: «{{damage}}». Оно пометило N восстановленных,
M ненадёжных и K нечитаемых фрагментов и перечислило такие затронутые строки (как видно → как
написано, вид):
- «снабже» → «снабже<restored>ния</restored>», hidden
- … (не больше 60 строк; дальше «… и ещё столько-то»)

`[если включён --second-pass-transcript:]`
Его транскрипция была такой:
<<<
…полный текст первого прохода…
>>>
Используй её только чтобы видеть, ГДЕ сомнительные места; каждое слово читай с картинки заново,
не копируй транскрипцию.

Теперь перечитай страницу с учётом этого. Сначала реши для затронутой области, СКРЫТЫ ли буквы
(обрезаны, ушли в корешок, закрыты) или ВИДНЫ, НО НЕНАДЁЖНЫ (размыты, сплющены, смазаны, выбелены
пересветом) — первое чтение могло назвать вид неверно. Скрытые буквы → восстанови по контексту и
видимым остаткам слова и оберни в `<restored>`; ненадёжные → `<fuzzy>` на КАЖДОМ слове, где хотя
бы одна буква прочитана по форме или контексту, а не по чёткой печати; `<unknown/>` — только там,
где уверенное восстановление невозможно. Дай по записи `edge_words` на КАЖДУЮ затронутую строку.
Если при осмотре страница на самом деле чистая — так и скажи (`damage` = "") и верни обычную
транскрипцию без тегов.

`[иначе — первый проход; подсказка: --hint + строка из --hints, а если их нет — фраза по умолчанию:]`
Сканы переплетённых томов могут иметь обрезанные, сплющенные или размытые буквы у корешка и
пересвеченные пятна в любом месте страницы. Про эту страницу ничего конкретного не сообщено,
так что ищи повреждения сам: реши по картинке, какой край, если такой есть, уходит в корешок
(это может быть и левый, и правый), и где печать ненадёжна. Везде, где буквы скрыты, обрезаны или
нечитаемы, ВОССТАНАВЛИВАЙ их по контексту и видимым остаткам слова и помечай восстановленные
буквы `<restored>`; видимые, но ненадёжные буквы помечай `<fuzzy>`; `<unknown/>` — только там,
где уверенное восстановление невозможно. Не помечай чистую печать и обычные переносы в конце
строки.

`[этап toc, годовой указатель:]`
Эта страница относится к ГОДОВОМУ УКАЗАТЕЛЮ статей, опубликованных в журнале за год (она может
продолжать предыдущую страницу и продолжаться на следующей). Перепиши её и заполни объект `toc`
с kind «index».

`[этап toc, оглавление выпуска:]`
Эта страница относится к ОГЛАВЛЕНИЮ выпуска (она может продолжать предыдущую страницу и
продолжаться на следующей). Перепиши её и заполни объект `toc` с kind «contents».

`[этап page:]`
Перепиши эту страницу.

`[всегда:]`
Строго применяй правило 6: в повреждённой зоне каждая буква, которую ты на самом деле не видел,
идёт в `<restored>…</restored>`, каждая буква, прочитанная по форме или контексту, а не по чёткой
печати, — в `<fuzzy>…</fuzzy>`, а фрагмент, который нельзя ни прочитать, ни восстановить,
становится `<unknown/>`. Написать восстановленную букву без тега — ошибка; пометить чистую печать —
тоже ошибка.
