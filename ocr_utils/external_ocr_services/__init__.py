"""Внешний OCR выпуска через VLM (DeepSeek V4.1 Flash): оглавление -> список статей -> остальные полосы.

ЗАЧЕМ. FineReader читает буквы, но плохо понимает структуру полосы: заголовки статей рассыпаются,
автор склеивается с текстом, таблицы с боковыми шапками превращаются в мешанину. Мультимодальная
модель отдаёт полосу сразу размеченным markdown; выбор модели и промпта сделан в
``research/external_ocr_models`` (DeepSeek V4.1 Flash, два куска, промпт v13). Здесь — боевой
прогон по паку, который знает про устройство журнала.

ЧТО ДЕЛАЕМ. Выпуск идёт в два этапа. Сперва — полосы «Содержания» и годового указателя (теги
CVAT из ``DB_REVIEWED``): из них извлекается структурированное оглавление — рубрики и статьи с
авторами. Затем остальные полосы с этим списком в промпте: ``#`` только у названий из списка,
``<rubric>`` только у рубрик, авторы после названия. Если модель на обычной полосе видит
оглавление, которого в базе нет, — предупреждение и по желанию перераспознание выпуска.
Повреждённые буквы всегда помечаются тегами TEI ``<supplied>``/``<unclear>``/``<gap>N</gap>`` (число
утраченных букв; заполнители «▒» расставляет разбор)
(бывший режим ``--damage`` стенда). Полоса оглавления приходит к модели как «предварительно
оглавление»: не признала — идёт ещё и как обычная. Полоса режется на тайлы по сетке в пикселях исходника: обычная полоса —
два, склеенный разворот — четыре.

ГРАНИЦА ЗАДАЧИ. Слияние с версией FineReader — отдельная задача; сравнение моделей и отчёты
остаются в исследовательском стенде.
"""

# Версия промптов и схемы ответа этого пакета; своя нумерация, отдельная от стенда. Пишется в
# .meta.json каждой полосы. Поднимать при любой правке шаблонов или схемы.
PROMPT_VERSION = 12
