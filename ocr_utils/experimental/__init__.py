"""Экспериментальный функционал внешнего OCR: живёт здесь, пока не пройдёт порог и не переедет в основные пакеты.

Правило подпакета: новая функциональность для `external_ocr_services` (и соседей) сначала
складывается сюда, гоняется стендом `scripts/replay_page.py` на повреждённом мини-наборе и
выпуске 1991/02, сравнивается с текущим поведением по числам
(`reports/external_ocr_damaged_research.md`), и только то, что прошло заранее названный порог,
предлагается к переносу в `external_ocr_services` — решение о переносе принимает пользователь.
Боевой конвейер отсюда ничего не импортирует.

Модули:

* склейка разорванных переносов по словарю прошла порог и переехала в
  ``external_ocr_services.hyphen_join`` (боевой шаг ``recognize_page``); здесь остались разметка
  корпуса и стенд сравнения (`scripts/compare_hyphen_join.py`);
* :mod:`strips` — нарезка полосы на горизонтальные полосы 1×N вместо квадратной сетки, чтобы
  каждая картинка укладывалась в потолок площади DeepSeek (≈ 1.8 Мпкс) без ужатия;
* :mod:`damage_hints` — подсказка о повреждении на страницу из CSV детектора корешка
  (`gutter_loss_detection`) вместо нейтральной фразы «ищи повреждения сам»;
* ``prompts_short/`` — укороченный промпт («ядро» v16 без таблиц, иллюстраций, формул и структуры)
  для проверки гипотезы, что длинный промпт перегружает модель на повреждённых полосах.

``mawo-pymorphy3`` для стенда сравнения — группа ``experimental`` в ``pyproject.toml`` (``uv sync --group experimental``).
"""
