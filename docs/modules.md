# Карта модулей

Файл сгенерирован `scripts/gen_module_map.py` из докстрингов — **не править руками**,
перегенерировать после добавления модуля или класса. Первая строка докстринга модуля и каждого
класса верхнего уровня; функции не включены — их ищите через `grep -n "^def "`.


## ocr_utils

ocr_utils — обработка сканов книг и журналов.

| Модуль | Назначение | Классы |
|---|---|---|
| `paper.py` | Уровень бумаги и отражение: общая нормировка скана, независимая от света. |  |
| `timing.py` | Утилита для замеров времени ключевых операций пайплайна и вывода их в лог. | `log_timing` — Контекстный менеджер: меряет длительность блока и пишет её в лог (INFO). |

## ocr_utils/background_smoothing

Сглаживание фона сканов перед бинаризацией в FineReader.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.background_smoothing``. |  |
| `cli.py` | CLI сглаживания фона: ``python -m ocr_utils.background_smoothing``. |  |
| `layout.py` | Защита растровых иллюстраций по разметке страницы (Surya layout). | `LayoutDetector` — Surya LayoutPredictor: блоки-иллюстрации на странице. |
| `pipeline.py` | Обход пачки файлов, обработка кадра и запись результата. | `SmoothParams` — Все настройки прогона (значения опций CLI в одном месте). |
| `processing.py` | Расчёт по кадру: защитная маска контента → размытие фона → композиция. | `SmoothResult` — Результат расчёта по кадру: итог, обе маски и разрешённые радиусы. |

## ocr_utils/db

| Модуль | Назначение | Классы |
|---|---|---|
| `migrate.py` | Переименование колонок и таблиц в уже заполненных базах разметки. | `MigrationReport` — Что случилось с одной базой. |
| `models.py` | Схема SQLite: пак -> год -> выпуск -> полоса -> (прямоугольные области \| маски \| точки). | `Base` (DeclarativeBase)<br>`Pack` (Base) — Пак сканов — папка вида ``.../Готовое/пак-1``, в CVAT ей отвечает проект.<br>`YearPackage` (Base) — Годовой комплект — подпапка пака вида ``1974``, в CVAT ей отвечает задача.<br>`Issue` (Base) — Выпуск — подпапка года вида ``05``, в CVAT ему отвечает джоб.<br>`Page` (Base) — Полоса выпуска — конкретный файл скана вместе с параметрами его уменьшения.<br>`RectRegion` (Base) — Прямоугольная область на полосе, координаты — ОРИГИНАЛА.<br>`MaskAnnotation` (Base) — Битовая маска на полосе (пока — только библиотечная печать).<br>`PointAnnotation` (Base) — Точка на полосе, координаты — ОРИГИНАЛА. Пока единственный вид — место экслибриса. |
| `repo.py` | Идемпотентная запись дерева пака и разметки в базу. |  |
| `session.py` | Открытие SQLite-базы: движок, ``PRAGMA foreign_keys``, создание недостающих таблиц. |  |

## ocr_utils/defocus_detection

Поиск расфокусированных кадров в папке со сканами.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: python -m ocr_utils.defocus_detection |  |
| `analysis.py` | Прогон метрик по файлам папки: чтение, тайлы, баллы, сводный ранг. | `FileResult` — Результат анализа одного файла. |
| `cli.py` | CLI: ранжирование сканов папки по качеству фокуса. |  |
| `image_io.py` | Чтение изображений и сбор входных файлов. |  |
| `labels.py` | Разбор ручной разметки брака: суффиксы в именах файлов и подпапки. | `Label` — Что человек сказал про кадр.<br>`Sample` — Кадр выборки вместе с разметкой и связями с другими кадрами. |
| `lines/detect.py` | Поиск областей строк текста через surya-ocr (``DetectionPredictor``). | `DetectParams` — Параметры детекции — они же часть ключа дискового кэша.<br>`DetectCache` — Дисковый кэш результатов детекции: один JSON на кадр.<br>`LineDetector` — Области строк текста на кадре. Модель грузится лениво и ровно одна. |
| `lines/measure.py` | Замер резкости по областям строк и отбор «мелкого текста». | `LineMeasurements` — Замеры по строкам одного кадра. |
| `lines/options.py` | Настройки режима «фокус по строкам» одним объектом. | `LineOptions` — Как мерить резкость по областям строк. |
| `lines/overlay.py` | Отладочные наложения: что именно детектор нашёл и что по этому намерено. |  |
| `lines/regions.py` | Область строки -> прямоугольные куски, по которым честно мерить резкость. | `LineRegion` — Область одной строки текста, найденная детектором.<br>`Chunk` — Прямоугольный кусок строки, вписанный в её полигон. |
| `lines/zonal_tiles.py` | Зональный расфокус по сетке тайлов: где на кадре мелкий текст поплыл. | `TileZonalResult` — Зональный расфокус кадра по сетке тайлов. |
| `metrics/base.py` | Общий интерфейс алгоритмов оценки резкости. | `Algorithm` — Описание одного алгоритма оценки резкости. |
| `metrics/cpbd.py` | CPBD — Cumulative Probability of Blur Detection. |  |
| `metrics/dom.py` | DOM (Difference of Median) — резкость края, нормированная на контраст. |  |
| `metrics/edge_dir.py` | Направленная резкость по спектру: отделяет смаз от движения от промаха фокуса. |  |
| `metrics/edge_width.py` | Ширина края штриха (Marziliano / JNB) — насколько «размазан» отдельный переход. |  |
| `metrics/hf_mid.py` | Доля высокочастотной энергии HF/MID — спектральная мера резкости. |  |
| `metrics/laplacian.py` | Наивная базовая метрика — дисперсия Лапласиана (variance of Laplacian). |  |
| `metrics/moire.py` | Энергия растрового муара, нормированная на контраст тайла. |  |
| `metrics/reblur.py` | Re-blur метрика Crete (The Blur Effect) — «сколько ещё осталось терять». |  |
| `report.py` | Печать отчёта: консольная таблица, markdown и CSV. | `LinkDirError` (Exception) — В целевой папке для симлинков лежит что-то, кроме симлинков. |
| `scale.py` | Масштаб и геометрия набора: шаг строк, локальный наклон, толщина штриха. | `TextScale` — Оценка масштаба набора по кадру. |
| `scoring.py` | Сведение карты тайлов в один балл файла и объединение метрик в сводный ранг. |  |
| `thresholds.py` | Абсолютные пороги расфокуса: теги тяжести вместо «покажи худшие N %». | `Preset` — Готовый набор порогов под конкретную метрику и конкретный режим агрегации. |
| `tiles.py` | Сетка тайлов и маска «печатных» (текстовых) тайлов. | `Grid` — Равномерная сетка тайлов по кадру. |
| `zonal.py` | Поиск ЗОНАЛЬНОГО расфокуса — когда часть кадра мягче остального. | `ZonalResult` — Оценка зонального расфокуса одного кадра. |

## ocr_utils/dewarp

Выпрямление страниц (dewarp): несколько движков под одним CLI.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.dewarp``. |  |
| `cli.py` | Командная строка: ``run`` — прогнать выбранные движки по полосам, папка на движок. | `Page` |
| `compare.py` | Пара «было \| стало» для просмотра глазами. |  |
| `engines/base.py` | Единый интерфейс движка выпрямления страниц. | `DewarpEngine` (ABC) — Базовый класс движка dewarp. |
| `engines/dewarpnet.py` | Движок DewarpNet (ICCV 2019) — перенос из корневого dewarp.py. | `DewarpNetEngine` (DewarpEngine) |
| `engines/docscanner.py` | Движок DocScanner (IJCV 2025) — основной для выпрямления страниц. | `DocScannerEngine` (DewarpEngine) |
| `engines/doctr.py` | Движок DocTr / GeoTr (ACM MM 2021) — перенос из корневого dewarp.py. | `DocTrEngine` (DewarpEngine) |
| `engines/doctr_plus.py` | Движок DocTr++ (TMM 2023) — «нерестриктед» ректификация документов. | `DocTrPlusEngine` (DewarpEngine) |
| `engines/download.py` | Утилиты загрузки: клон апстрим-репозиториев и скачивание весов моделей. |  |
| `engines/pagedewarp.py` | Движок page-dewarp (Matt Zucker / lmmx) — классический алгоритм без нейросети. | `PageDewarpEngine` (DewarpEngine) — Расширенный вариант: поля 0 (ничего не режется по бокам), ширина ровно как у входа, |
| `engines/textline.py` | Движок ``textline`` — выпрямление по центр-линиям строк, как dewarp в Leptonica. | `Field` — Поле вертикальной диспаратности на сетке — для отладки и тестов.<br>`TextLineEngine` (DewarpEngine) |
| `engines/uvdoc.py` | Движок UVDoc (SIGGRAPH Asia 2023) — neural grid-based unwarping. | `UVDocEngine` (DewarpEngine) |
| `quality.py` | Безэталонная оценка выпрямления: метрики кривизны до и после. | `QualityRow` |

## ocr_utils/docx_md

Подпакет конвертации DOCX → Markdown и нарезки Markdown на куски для LLM.

| Модуль | Назначение | Классы |
|---|---|---|
| `docx_to_md.py` | Экспорт DOCX-документов в Markdown. |  |
| `split_md_by_articles.py` | Нарезка MD-файла (OCR журнала/книги) на куски по статьям для подачи в LLM. | `Unit` — Атомарная (неделимая) единица документа — статья, рубрика или обложка. |

## ocr_utils/external_ocr_services

Внешний OCR выпуска через VLM (DeepSeek V4.1 Flash): оглавление -> список статей -> остальные полосы.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``uv run python -m ocr_utils.external_ocr_services <команда>``. |  |
| `cli.py` | Команды пакета: ``uv run python -m ocr_utils.external_ocr_services <команда>``. |  |
| `client.py` | Клиент OpenRouter поверх штатного SDK ``openrouter``: один вызов chat/completions с ретраями и учётом цены. | `OpenRouterError` (RuntimeError) — Сбой запроса: HTTP-статус (если был), начало тела ответа; сеть — без статуса.<br>`ChatResponse` — Разобранный ответ chat/completions: текст, кто обслужил, токены, цена, время, число попыток.<br>`OpenRouterClient` — Синхронный клиент: ``chat(payload)`` с ретраями; ``http_client`` — подмена httpx в тестах. |
| `models.py` | Реестр OpenRouter-моделей боевого прогона: короткое имя, id, режим JSON, рассуждения, провайдеры. | `JsonMode` (StrEnum) — Как просить структурированный ответ; значения — как в ``response_format.type`` OpenRouter.<br>`Reasoning` (StrEnum) — Уровень рассуждений модели.<br>`ModelSpec` — Запись реестра: как зовут модель у нас и у OpenRouter, как с ней разговаривать и почём. |
| `ocr.py` | Одна полоса → один запрос к модели → PageResult, файлы выхода и .meta.json. | `RunOptions` — Настройки прогона, общие для всех полос: тайлы, модель, второй проход, отладочный выход.<br>`SecondPass` — Что первый проход сказал о повреждениях — подсказка для второго прохода той же полосы.<br>`PageJob` — Что распознать: полоса, этап и известное оглавление выпуска (для этапа ``page``).<br>`SecondPassReason` (StrEnum) — Почему полоса ушла во второй проход; пишется в meta (``second_pass_reason``) и summary.csv.<br>`PassChoice` (StrEnum) — Какой проход ушёл в финал (``second_pass_chosen`` в meta) и суффикс его файлов в debug-dir.<br>`OutputPaths` — Файлы одной полосы под out-dir.<br>`DebugPaths` — Файлы одной полосы под debug-dir (``--debug-dir``); для второго прохода — с суффиксом ``.pass2``. |
| `pages.py` | Полосы на входе и что о них знает база: обход папки, списки, флаги оглавления. | `PageFlags` — Что база знает о полосе. ``known=False`` — записи нет, полоса считается обычной. |
| `pipeline.py` | Обход пака по выпускам: полосы оглавления -> список статей -> остальные полосы -> fallback. | `OnMissedToc` (StrEnum) — Что делать, когда модель увидела оглавление на полосе, которую база считает обычной (``--on-missed-toc``).<br>`RedoScope` (StrEnum) — Какие обычные полосы запрашивать заново на круге повтора выпуска (``--redo-scope``).<br>`RedoReason` (StrEnum) — Почему полоса идёт в сеть заново на круге повтора; пишется в лог по полосе.<br>`PipelineParams` — Параметры прогона из CLI: пути, теги полос, параллелизм, отбор входа, поведение fallback.<br>`PipelineStats` — Счётчики прогона для итоговой строки лога и тестов; копятся по всем выпускам. |
| `render.py` | Разложить PageResult в .md: YAML-шапка с полями полосы и тело в markdown. |  |
| `schema.py` | Ответ модели на одну полосу: поля, JSON-схема по этапу и терпимый разбор. | `Stage` (StrEnum) — Этап распознавания полосы: ``TOC`` — полоса оглавления/указателя (извлечь структуру),<br>`TocKind` (StrEnum) — Вид полосы по мнению модели или базы; совпадает с видами детектора ``scan_markup.toc``.<br>`AuthorArticle` (StrEnum) — Чья это подпись на обычной полосе; по ней пост-обработка (``structure``) ставит ``<author>``.<br>`AuthorPrinted` (StrEnum) — Где на полосе напечатано имя автора: над заголовком, рядом, под ним, в конце текста, в колонтитуле.<br>`EdgeKind` (StrEnum) — Вид повреждения строки в ``edge_words``: буквы скрыты/срезаны и восстановлены (``HIDDEN``),<br>`DamageTag` (StrEnum) — Теги повреждений в тексте по TEI: ``<supplied>`` — буквы не видны, вписаны по контексту;<br>`StructureTag` (StrEnum) — Теги структуры: рубрика статьи (только из оглавления), рубрика внутри оглавления, автор,<br>`BlockTag` (StrEnum) — Теги-обёртки блоков: список оглавления и сноска; считаются в meta (``blocks``).<br>`IllustrationKind` (StrEnum) — Вид иллюстрации — первая строка fenced-блока в квадратных скобках: ``[фотография]``,<br>`ParseError` (ValueError) — Ответ модели не удалось привести к PageResult.<br>`TocArticle` — Статья в оглавлении: название, авторы, номер страницы и (в указателе) номер выпуска.<br>`TocSection` — Секция оглавления: рубрика (``None`` — без рубрики) и её статьи по порядку.<br>`TocPage` — Структурированное оглавление ОДНОЙ полосы; слияние полос выпуска — в ``toc.py``.<br>`PageResult` — Разобранный ответ модели на одну полосу — то, что уходит в ``.json`` и ``.md``.<br>`EdgeWordsReport` — Что сделал код по списку ``edge_words``: сколько тегов вставил в тело и сколько записей отбросил как пустые. |
| `structure.py` | Пост-обработка markdown полосы: `#` только из оглавления, авторы при своей статье, рубрика перед `#`. | `StructureReport` — Что изменила пост-обработка — попадает в .meta.json (``structure``). |
| `tiling.py` | Нарезка полосы на тайлы по сетке в пикселях исходника и подготовка каждого к отправке. | `TileBox` — Один тайл сетки: позиция в сетке и прямоугольник в пикселях исходника.<br>`PreparedImage` — Готовый к отправке тайл: JPEG-байты, размер после уменьшения и его место в сетке.<br>`TileSummary` — Один тайл в сводке сетки для .meta.json.<br>`GridSummary` — Сводка сетки тайлов полосы: сколько столбцов и строк и что за тайлы; пишется в .meta.json. |
| `toc.py` | Оглавление выпуска: слияние полос, рубрики-продолжения, списки для промпта и их отпечаток. | `IssueToc` — Слитое оглавление одного вида (contents или index) по всем его полосам выпуска.<br>`ParsedEntry` — Пункт блока ``<toc>`` тела после разбора.<br>`TocReconcile` — Итог сверки тела полосы оглавления с объектом ``toc``. |

## ocr_utils/gutter_loss_detection

Поиск разворотов, у которых текст ушёл под переплёт при тугой подшивке.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: python -m ocr_utils.gutter_loss_detection |  |
| `analysis.py` | Прогон детектора по папке: чтение, разбор разворота, баллы, сортировка. | `SideResult` — Результат по одной полосе разворота.<br>`FileResult` — Результат по кадру. |
| `cli.py` | CLI: поиск разворотов, у которых текст ушёл под переплёт. |  |
| `geometry.py` | Разбор разворота: линия сгиба, строки, внутренние поля обеих полос. | `SideGeometry` — Внутреннее поле одной полосы разворота.<br>`SpreadGeometry` — Разбор кадра-разворота. |
| `metrics.py` | Балл «текст ушёл под корешок» и признак таблицы у сгиба. | `Verdict` — Что делать с кадром. |
| `report.py` | Отчёты: консольная таблица, CSV, markdown, папка симлинков, лист врезок. |  |

## ocr_utils/gutter_loss_restoration

Восстановление текста, срезанного корешком при тугой подшивке.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: python -m ocr_utils.gutter_loss_restoration |  |
| `cli.py` | CLI: восстановление текста, срезанного корешком, по папке со сканами. |  |
| `compose.py` | Вклейка восстановленных букв: место, бумага, литеры. | `Placement` — Одна вклейка. |
| `glyphs.py` | Библиотека литер, собранная из самой полосы. | `Glyph` — Литера-донор. |
| `layout.py` | Вёрстка строки по краске: где кончается строка, где слова, где буквы. | `Group` — Связная группа краски (слово или буква). |
| `lexicon.py` | Словарь выпуска, собранный из распознанных полос. |  |
| `library.py` | Библиотека литер выпуска: собирается один раз по всей папке. |  |
| `ocrcache.py` | Кэш распознавания полос: один JSON на кадр. |  |
| `pageocr.py` | Чтение полос разворота через surya: текст строк и боксы слов. | `Word` — Слово строки.<br>`Line` — Распознанная строка полосы. |
| `pipeline.py` | Пакетный прогон восстановления: словарь, библиотека, кадры, отчёт. |  |
| `restore.py` | Восстановление одного разворота: что съел корешок — то и дописываем. | `Row` — Строка полосы: полоса краски, текст и группы слов.<br>`LineReport` — Что сделано (или не сделано) со строкой. |
| `tails.py` | Что именно съел корешок в конце строки. | `Tail` — Решение по хвосту одной строки. |

## ocr_utils/inpainting

Закрас областей по маске — общий для пальцев и для разметки из CVAT.

| Модуль | Назначение | Классы |
|---|---|---|
| `apply.py` | Общий цикл закраса: маска → группы → ROI → заливка → вклейка. |  |
| `backends.py` | Сменные заливщики ROI: LaMa и Stable Diffusion за одним интерфейсом. | `RoiFiller` (Protocol) — Заливка одного ROI. ``bounds`` — (x1, y1, x2, y2) ROI в координатах кадра.<br>`SdParams` — Параметры прогона Stable Diffusion.<br>`LamaFiller` — Заливка LaMa. Промпта у неё нет, ``bounds`` не используется.<br>`SdFiller` — Заливка Stable Diffusion с промптом, выбранным по положению зоны на полосе. |
| `grouping.py` | Группировка связных областей маски в «операции закраса». |  |
| `roi.py` | Геометрия ROI вокруг закрашиваемой области и растушёванное вклеивание результата. |  |

## ocr_utils/legacy

Помойка: неподдерживаемый код и заброшенные эксперименты.

| Модуль | Назначение | Классы |
|---|---|---|
| `cli.py` | CLI-точка входа: python -m ocr_utils или ocr-utils. |  |
| `config.py` | Общие константы и конфигурация ocr_utils. |  |
| `defocus_detection/__main__.py` | Точка входа: ``python -m ocr_utils.legacy.defocus_detection``. |  |
| `defocus_detection/detect_defocus.py` | CLI детектора расфокуса для сканов газет (RAF с JPEG-превью или обычные JPEG). |  |
| `defocus_detection/fft_hf.py` | Независимый метод HF/MID — доля высокочастотной энергии (для кросс-проверки, этап E). |  |
| `defocus_detection/grid.py` | Разбиение кадра на равномерную сетку тайлов. |  |
| `defocus_detection/image_io.py` | Ввод/вывод: загрузка превью из RAF/JPEG, сбор входных файлов, тепловые карты. |  |
| `defocus_detection/laplacian.py` | Альтернативный метод резкости — дисперсия Лапласиана по тайлам. |  |
| `defocus_detection/moire.py` | Основной метод детектора — энергия растрового муара (+ нормировка A, гейт B, зона C). |  |
| `defocus_detection/pipeline.py` | Оркестрация: прогон выбранного метода по тайлам + нормировка (A), зона (C), |  |
| `dewarp/__main__.py` | Точка входа: ``python -m ocr_utils.legacy.dewarp``. |  |
| `dewarp/dewarp_pages.py` | CLI геометрического выпрямления страниц (распрямление строк). |  |
| `dewarp/engines/base.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.base``. |  |
| `dewarp/engines/dewarpnet.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.dewarpnet``. |  |
| `dewarp/engines/docscanner.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.docscanner``. |  |
| `dewarp/engines/doctr.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.doctr``. |  |
| `dewarp/engines/doctr_plus.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.doctr_plus``. |  |
| `dewarp/engines/download.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.download``. |  |
| `dewarp/engines/pagedewarp.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.pagedewarp``. |  |
| `dewarp/engines/uvdoc.py` | Тонкая обёртка: движок переехал в ``ocr_utils.dewarp.engines.uvdoc``. |  |
| `finger_removal/__main__.py` | Точка входа: ``python -m ocr_utils.legacy.finger_removal``. |  |
| `finger_removal/cli.py` | CLI удаления пальцев со сканов: маска (нейросеть/кожа) → инпейнтинг (LaMa/SD). |  |
| `finger_removal/detect_fingers.py` | Обводка пальцев/рук на сырых сканах криволинейной красной границей. |  |
| `finger_removal/inpainting.py` | Инпейнтинг области под пальцем. |  |
| `finger_removal/masking_batch.py` | Батчевая детекция пальцев и отладочный оверлей маски (помойка). |  |
| `finger_removal/sd_inpaint.py` | Инпейнтинг пальца через Stable Diffusion + диспетчер методов (помойка). |  |
| `ocr.py` | OCR-слой: двухэтапный подход для максимального качества распознавания без изменения изображений. |  |
| `page_layout_processing/check_opencv_segment_layout.py` | Тестирование ocrd-segment для определения границ полезной области на страницах. |  |
| `page_layout_processing/check_surya_layout.py` | Тестирование surya-ocr для определения границ полезной области на страницах. |  |
| `pdf_utils_flat.py` | Сборка PDF из пачки JPEG без перекодирования (img2pdf); старая плоская версия pdf_utils. |  |
| `pipeline.py` | Основной пайплайн: обработка одного PDF и рекурсивная обработка директории. |  |
| `run_ocr_batch.py` | Скрипт для массовой оцифровки PDF-файлов из директории. |  |
| `stamp_and_writing_detection/detect_stamps_writing.py` | Детектирование библиотечных штампов и рукописных надписей на сканах. | `Detection` — Одна найденная область.<br>`MethodStats` — Накопленная статистика по одному методу для итогового отчёта. |

## ocr_utils/line_art_detection

Поиск страниц с крупным штриховым рисунком в бинаризованных PDF.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.line_art_detection``. |  |
| `analysis.py` | Обход папки с PDF: рендер страниц, признаки, пул процессов. | `PageResult` — Итог по одной странице PDF; ровно то, что уезжает из воркера и ложится в CSV. |
| `cli.py` | Командная строка детектора: тяжёлый ``scan`` и дешёвый ``export``. |  |
| `export.py` | Выгрузка найденных страниц картинками — по папке на выпуск. |  |
| `features.py` | Признаки связного пятна краски и правило «это крупный штриховой рисунок». | `LineArtParams` — Пороги детектора при КОНКРЕТНОМ разрешении страницы.<br>`Candidate` — Одно пятно, признанное штрихом, вместе с признаками, по которым его признали.<br>`PageFindings` — Итог по одной странице. |
| `layout.py` | Предложения разметки страницы от Surya: таблицы без линеек и многострочные формулы. | `LayoutProposals` — Surya LayoutPredictor: блоки страницы вместе с их классами. |
| `markup.py` | Растровые области из базы разметки — в координатах страницы бинаризованного PDF. | `PdfPageMarkup` — Разметка одной страницы PDF: прямоугольники растра в пикселях СКАНА плюс поля. |
| `render.py` | Рендер страницы PDF в полутоновый массив. |  |
| `report.py` | Отчёты детектора: CSV для калибровки, markdown и консольная таблица для чтения. |  |

## ocr_utils/misc

| Модуль | Назначение | Классы |
|---|---|---|

## ocr_utils/pdf_utils

Утилиты для работы с PDF-файлами.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | CLI-точка входа: python -m ocr_utils.pdf_utils. |  |
| `collect_sharpened.py` | Сбор выгрузки Capture One из подпапок ``sharpened`` в обычное дерево пака. | `IssuePlan` — Что делать с одним выпуском.<br>`CollectReport` — Итоги прогона. |
| `detect_mrc_leftovers.py` | Поиск страниц, где при MRC-сжатии часть текста «провалилась» в фоновый слой. | `Blob` — Одно найденное пятно — предположительно символ, оставшийся в фоне.<br>`PageResult` — Результат анализа одной страницы PDF. |
| `extract_images.py` | Извлечение изображений из PDF без перекодирования. | `BrokenPdfError` (RuntimeError) — В PDF потеряны страницы, и его выгрузка выброшена целиком.<br>`ExtractionReport` — Итог рекурсивного обхода.<br>`_PdfJob` — Одна задача для воркера: какой PDF куда разобрать и с какими параметрами. |
| `final_pdfs.py` | Финальные PDF: распознанные страницы плюс возвращённые на место иллюстрации. | `AssembleParams` — Параметры прогона, едущие в воркер вместе с планом выпуска.<br>`IssueReport`<br>`AssembleStats` |
| `intermediate_pdfs.py` | Промежуточные PDF по выпускам — то, что уходит в пакетное распознание FineReader. | `PicturePlan` — Размеченная иллюстрация: место в пикселях оригинала и цветность.<br>`PagePlan` — Полоса и всё, что нужно, чтобы положить её на страницу PDF.<br>`IssuePlan` — Выпуск целиком — одна задача пула.<br>`IssueReport` — Что случилось с одним выпуском.<br>`BuildParams` — Параметры прогона, едущие в воркер вместе с планом выпуска.<br>`BuildStats` — Итоги прогона. |
| `jpeg_pdf.py` | Страницы PDF из JPEG без перекодирования и врезки поверх уже готовых страниц. | `JpegError` (ValueError) — JPEG не годится для вставки в PDF (не JPEG, обрезан, прогрессивный).<br>`JpegInfo` — Размеры и цветность JPEG, снятые с маркеров без декодирования пикселей.<br>`Overlay` — Врезка: готовые байты JPEG и место под них в пикселях оригинала. |
| `padding.py` | Добавление полей к экспортируемым из PDF изображениям. | `LosslessPaddingError` (RuntimeError) — Беспотерьное добавление полей к JPEG невозможно для этого файла. |
| `verify_sharpened.py` | Сверка заострённых копий с их полосами: та ли картинка лежит под этим именем. | `PageCheck` — Что проверяем по одной полосе.<br>`CheckResult` — Итог по одной полосе.<br>`VerifyStats` |

## ocr_utils/rotated_text

Повёрнутый текст на полосе: то, что FineReader не читает, переписываем прямо.

| Модуль | Назначение | Классы |
|---|---|---|
| `tables/__main__.py` | Точка входа: ``python -m ocr_utils.rotated_text.tables``. |  |
| `tables/cli.py` | Прогон по паку: три фазы, между которыми — surya в родителе. |  |
| `tables/dpi.py` | Минимальный читаемый кегль и требуемый DPI страницы. |  |
| `tables/fit.py` | Шрифт замены и укладка текста в ячейку. | `Fit` — Уложенный текст: каким кеглем, какими строками и сколько места занял. |
| `tables/ocr.py` | Чтение ячейки tesseract-ом: подпроцесс, TSV, строки с уверенностью. | `CellText` — Что прочитано в одной ячейке. |
| `tables/orientation.py` | Повёрнут ли текст в ячейке и в какую сторону. | `CellOrientation` — Вердикт по ячейке. ``rotate_cw`` — на сколько повернуть по часовой, чтобы текст стал |
| `tables/pipeline.py` | Цепочка на одну таблицу: разбор (CPU, в пуле) и перекройка с набором (CPU, в пуле). | `Options`<br>`CellRecord` — Всё, что известно про ячейку: геометрия (в рабочем dpi), поворот, текст, набор.<br>`TableAnalysis` — Результат первой фазы. ``grid`` и ``native_font_px`` — в рабочем dpi.<br>`TableRewrite` — Результат второй фазы, без картинок — они отдаются рядом.<br>`RewriteImages`<br>`_Plan` — Один из вариантов перекройки: чем он кончается и что в нём набирать. |
| `tables/render.py` | Закраска старого текста и набор нового. | `Replacement` — Что набрать в ячейке: внутренность, укладка, уровень бумаги для закраски. |
| `tables/reshape.py` | Перекройка таблицы, когда увеличения страницы не хватает. |  |
| `tables/second_opinion.py` | Второе мнение surya для каждой повёрнутой ячейки — с фильтром выдумок. |  |
| `tables/sheets.py` | Листы «было — стало»: проверка глазами того, ради чего всё и затевалось. |  |
| `tables/source.py` | Откуда берутся таблицы: строки ``rect_regions`` базы разметки и вырезки из полос. | `TableRef` — Одна таблица из базы: где лежит полоса и где на ней рамка.<br>`TableCrop` — Вырезка таблицы: повёрнута как полоса, выровнена по линейкам, в исходном dpi. |
| `tables/structure.py` | Сетка ячеек вырезанной таблицы и всё, что о ней можно измерить без распознавания. | `TableStructure` — Сетка и уровни бумаги и краски по всей таблице. |

## ocr_utils/scan_cleanup

Очистка пака сканов по разметке, уточнённой руками в CVAT.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.scan_cleanup <команда>``. |  |
| `cli.py` | CLI подсистемы очистки пака: ``python -m ocr_utils.scan_cleanup <команда>``. |  |
| `compare.py` | Инструменты сравнения: выбрать параметры глазами, а не на веру. | `CompareMasksParams`<br>`CompareInpaintParams` |
| `inpaint.py` | Операция 1: закрас размеченных в CVAT областей (печати, надписи, прочее). | `InpaintOptions` — Настройки закраса.<br>`InpaintReport` — Что и чем закрашено — для оверлея, лога и отчёта. |
| `naming.py` | Имена очищенных полос: к имени файла дописывается отпечаток исходника. |  |
| `overlay.py` | Debug-оверлеи: что закрасили и что защитили от размытия. Всегда JPG. |  |
| `prompts.py` | Промпты Stable Diffusion и правило выбора по тому, куда попала зона закраса. | `PromptSet` — Формулировки промптов; каждая переопределяется опцией CLI. |
| `protect.py` | Защитная маска из размеченных в базе растровых областей. | `ProtectOptions` — Что попадает в защитную маску размытия. |
| `runner.py` | Прогон по паку: чтение полосы, закрас, размытие, запись — за один проход. | `CleanupParams` — Все настройки прогона в одном месте (значения опций CLI).<br>`PageReport` — Что случилось с одной полосой. |
| `smoothing.py` | Операция 2: размытие фона по защитной маске из базы. | `SmoothOptions` — Настройки размытия фона. |
| `source.py` | Разметка пака из sqlite — в плоские, пиклуемые структуры. | `Rect` — Растровая область полосы в координатах оригинала.<br>`MaskRow` — Строка маски под удаление: охватывающий прямоугольник и RLE.<br>`PageMarkup` — Всё, что нужно знать о полосе, чтобы её обработать, — без обращения к базе. |

## ocr_utils/scan_cropping

Вырезка сканов: детекция разворота, правильный поворот и crop.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.scan_cropping``. |  |
| `background_fill.py` | Заливка областей кадра, где нет содержимого книги. |  |
| `cli.py` | CLI вырезки сканов: ``python -m ocr_utils.scan_cropping``. |  |
| `cropping.py` | Вырезка crop-зоны из кадра двумя способами (см. ``--crop-mode``). |  |
| `finger_removal/asymmetric_dilation.py` | Асимметричная (разная по X и Y) дилатация зон пальца. | `FingerZoneDilation` — Считает меру близости зоны пальца к сторонам кадра и коэффициенты дилатации. |
| `finger_removal/docshadow_net/backbone.py` | Сеть DocShadow (снятие теней с документа): базовые слои — LayerNorm2d, блоки внимания. Перенос из апстрима. | `LayerNormFunction` (Function)<br>`LayerNorm2d` (Module)<br>`SimpleGate` (Module)<br>`DFEBlock` (Module)<br>`DFE` (Module) |
| `finger_removal/docshadow_net/blocks.py` | Сеть DocShadow: свёрточные и residual-блоки. Перенос из апстрима. | `ConvLayer` (Module)<br>`Aggreation` (Module)<br>`SelfAttention` (Module)<br>`SPP` (Module) |
| `finger_removal/docshadow_net/model.py` | Сеть DocShadow: сборка модели из блоков backbone/blocks. Перенос из апстрима. | `ResidualBlock` (Module)<br>`LapPyramidConv` (Module)<br>`TRM` (Module)<br>`BiasFree_LayerNorm` (Module)<br>`WithBias_LayerNorm` (Module)<br>`LayerNorm` (Module)<br>`NextAttentionImplZ` (Module)<br>`NextAttentionZ` (Module)<br>`FeedForward` (Module)<br>`TransformerBlock` (Module)<br>`OverlapPatchEmbed` (Module)<br>`Downsample` (Module)<br>`Upsample` (Module)<br>`TAA` (Module) — Layer attention module<br>`Backbone` (Module)<br>`Model` (Module) |
| `finger_removal/finger_shadow.py` | Коррекция теневой зоны вокруг пальца несколькими методами (выбор в CLI). |  |
| `finger_removal/masking.py` | Построение маски пальцев на сканах. |  |
| `finger_removal/removal.py` | Удаление придерживающего страницу пальца из кадра: детекция → закраска. |  |
| `finger_removal/text_protection.py` | Защита контентных зон (page layout через Surya) от закраски пальца. |  |
| `geometry.py` | Геометрия crop-зоны: правильный поворот, повёрнутый bbox, подготовка масок. |  |
| `gpu_models.py` | Единая точка входа во все нейросети пайплайна: класс ``GpuModels``. | `GpuModels` — Все нейромодели пайплайна вырезки сканов и операции над ними. |
| `image_io.py` | Сбор входных файлов и сохранение результата в нужном формате. |  |
| `layout_filtering.py` | Отсев паразитных (артефактных) блоков Surya layout из расчёта crop-зоны. |  |
| `levels.py` | Компенсация уровней (контраст-стретч) по гистограмме — штатными средствами OpenCV. |  |
| `morphology.py` | Морфология бинарных масок ДИСКОМ — через distance transform, а не ядром. |  |
| `overlay.py` | Debug-оверлей: что нашёл детектор и что будет вырезано. |  |
| `page_detection.py` | Детекция разворота книги в кадре: YOLO-World боксы → SAM силуэт → маска. |  |
| `pipeline.py` | Оркестрация: настройки прогона, обработка одного кадра, обход пачки файлов. | `CropParams` — Все настройки прогона (значения опций CLI в одном месте). |

## ocr_utils/scan_markup

Разметка полос журнала: растровые области и библиотечные печати.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``python -m ocr_utils.scan_markup``. |  |
| `cli.py` | CLI подсистемы разметки полос: ``python -m ocr_utils.scan_markup <команда>``. |  |
| `curved_lines/__main__.py` | Точка входа: ``python -m ocr_utils.scan_markup.curved_lines``. |  |
| `curved_lines/analysis.py` | Прогон детекторов по дереву полос. | `PageResult` — Что известно об одной полосе после всех детекторов.<br>`Job` |
| `curved_lines/cache.py` | Дисковый кэш измерений: один JSON на полосу и детектор. | `PageCache` |
| `curved_lines/cli.py` | Командная строка: ``run`` — прогон по дереву полос, ``report`` — пересборка отчётов из CSV. |  |
| `curved_lines/detectors/base.py` | Общий контракт детекторов кривизны строк. | `Measure` — Мнение одного детектора об одной полосе.<br>`GpuPage` — Полоса на GPU-этап: картинка уже уменьшена воркером и разжата в родителе.<br>`BatchDetector` (Protocol) — GPU-детектор: модель грузится лениво, вход — пачка полос.<br>`Detector` — Запись в реестре детекторов. |
| `curved_lines/detectors/end_curl.py` | Загнутые кончики строк: усреднённый по колонке профиль отклонения концов строк. |  |
| `curved_lines/detectors/line_fit.py` | Аппроксимация каждой строки прямой и параболой — по образцу dewarp из Leptonica. | `LineSample` — Строка после сегментации: бокс на копии 150 dpi и центр-линия на копии 300 dpi. |
| `curved_lines/detectors/skew_map.py` | Карта локальных углов: наклон строк по тайлам полосы. |  |
| `curved_lines/detectors/strip_shift.py` | Сдвиг строк между соседними вертикальными полосами колонки — кросс-корреляция профилей. |  |
| `curved_lines/detectors/surya_lines.py` | Кривизна строк по полигонам детектора Surya — нейросетевая сегментация вместо RLSA. | `SuryaCurves` — GPU-детектор: полигоны Surya (через LineDetector с кэшем) плюс краска внутри них. |
| `curved_lines/finereader_compare.py` | Пары «было \| стало»: исходная полоса против страницы, в которую её превратил FineReader. | `Pair` |
| `curved_lines/fitting.py` | Математика, общая для детекторов: центр-линия строки, её аппроксимации и сводки по полосе. | `LineFit` — Аппроксимации одной строки. |
| `curved_lines/flags.py` | Пороги, флаги, score и сводный вердикт. | `ThresholdError` (ValueError) — Опечатка в ``--thr``: неизвестный детектор, метрика или не число.<br>`Thresholds` |
| `curved_lines/labels.py` | Разметка «кривая / прямая» и таблица разделения по метрикам. | `Label`<br>`Separation` — Насколько одна метрика разводит классы. |
| `curved_lines/overlay.py` | Отладочный оверлей: что каждый детектор увидел на полосе. | `OverlayJob` |
| `curved_lines/report.py` | Отчёты по прогону: каталог симлинков, CSV, markdown, контактный лист. | `LinkDirError` (Exception) — В каталоге симлинков нашлось что-то, кроме симлинков. |
| `cvat/client.py` | Подключение к локальному CVAT: чтение ``docker/.env`` и создание клиента. | `CvatSettings` — Адрес, учётка и организация CVAT. |
| `cvat/export.py` | Команда ``from-cvat``: уточнённая разметка из CVAT -> вторая база той же схемы. | `ExportParams` — Параметры прогона ``from-cvat``.<br>`ExportStats` — Итоги прогона.<br>`CopyStats` — Итоги ``copy-regions``. |
| `cvat/images.py` | Уменьшенные копии полос для разметки в CVAT. | `ImageJob` — Одна полоса: что откуда куда уменьшать.<br>`ImageResult` — Результат по одной полосе. |
| `cvat/project.py` | Заведение проекта, задач и джобов CVAT по содержимому базы. |  |
| `cvat/publish.py` | Команда ``to-cvat``: база -> уменьшенные картинки в share -> проект/задачи/джобы. | `PublishParams` — Параметры прогона ``to-cvat``.<br>`PublishStats` — Итоги прогона. |
| `cvat/shapes.py` | Счёт шейпов по задачам CVAT: снимок до операции и сверка после. |  |
| `detection/boxes.py` | Алгебра прямоугольников разметки: слияние, отсев мелочи, признак «во всю полосу». |  |
| `detection/color_kind.py` | Цветная ли растровая область или полутоновая. | `ColorKind` — Решение color/grayscale вместе со всеми тремя измерениями. |
| `detection/cover.py` | Обложка выпуска: первая полоса и детектор сплошной плашки. |  |
| `detection/dots.py` | Поиск полутоновой (растровой) печати по статистике пятен краски на ПОЛНОМ кадре. | `ScreenParams` — Пороги детектора при КОНКРЕТНОМ разрешении полосы.<br>`CellMaps` — Статистика пятен, собранная по клеткам кадра.<br>`ScreenRegions` — Связные полутоновые области полосы: карта меток по клеткам и их прямоугольники. |
| `detection/layout_cache.py` | Кэш разметки surya layout на диске: pickle на полосу, та же раскладка папок, что у пака. | `CachedLayout` — Разметка полосы в пикселях картинки ``layout.width`` x ``layout.height``.<br>`_Stub` — Заглушка вместо класса чужого пакета: принимает любое состояние и ничего не делает.<br>`_LenientUnpickler` (Unpickler) |
| `detection/overlay.py` | Отладочные оверлеи: уменьшенная полоса с обведёнными областями и подписями. |  |
| `detection/page.py` | Детекция по ОДНОЙ полосе: файл на диске -> результат, без единого обращения к базе. | `PageOptions` — Всё, что нужно детекции по одной полосе. Обязано переживать pickle.<br>`DetectedRegion` — Одна найденная область в координатах ОРИГИНАЛА.<br>`PageAnalysis` — Пиксельный разбор полосы: всё, что посчитано без GPU. Едет из воркера в родителя.<br>`PageResult` — Результат по одной полосе. ``error`` непуст — значит остальное недостоверно. |
| `detection/recolor.py` | Пересчёт по УЖЕ найденным областям, без повторной детекции. | `RecolorParams` — Параметры ``recolor`` и ``mark-covers``.<br>`RecolorStats` — Итоги: сколько полос прочитано и сколько областей сменило решение.<br>`_Job` — Полоса и её области в координатах оригинала.<br>`_Result` |
| `detection/regions.py` | Сборка растровых областей полосы: Surya предлагает, пиксели уточняют. | `Finding` — Одна найденная область: прямоугольник ОРИГИНАЛА и чем она оказалась.<br>`RasterFindings` — Что нашлось на полосе. |
| `detection/run.py` | Прогон предварительной детекции по паку: оригиналы -> SQLite. | `DetectParams` — Параметры прогона ``detect``.<br>`DetectStats` — Итоги прогона — то, что печатается в конце.<br>`_Job` — Задание воркеру: что считать и чем. |
| `detection/tone.py` | Отличает полутоновую печать от штрихового рисунка по тонам и по растровой сетке. | `ToneMaps` — Карты тонов и растровой сетки по полосе. Едет из воркера в родителя.<br>`ToneStats` — Признаки по одному прямоугольнику. |
| `geometry.py` | Пересчёт координат между оригиналом и уменьшенной копией для CVAT. |  |
| `hashing.py` | Хеши исходных сканов: чем считаем и как решаем, что файл не менялся. | `FileStamp` — Отпечаток файла: дешёвая часть (``stat``) и дорогая (хеш содержимого). |
| `orientation/__main__.py` | Точка входа: ``python -m ocr_utils.scan_markup.orientation``. |  |
| `orientation/analysis.py` | Прогон детекторов по дереву полос. | `PageResult` — Что известно об одной полосе после всех детекторов.<br>`Job` |
| `orientation/cli.py` | Командная строка подпакета: ``run`` — прогон по дереву, ``validate`` — проверка. |  |
| `orientation/detectors/base.py` | Общий контракт детекторов ориентации. | `Verdict` — Мнение одного детектора об одной полосе.<br>`Frame` — Подготовленная полоса: один раз разжата, дальше её делят все CPU-детекторы.<br>`BatchDetector` (Protocol) — GPU-детектор: модель грузится лениво, вход — пачка картинок.<br>`Detector` — Запись в реестре детекторов. |
| `orientation/detectors/doctr_cls.py` | Классификатор ориентации страницы из docTR (``mobilenet_v3_small_page_orientation``). | `DoctrOrientation` — Пачка картинок на вход, вердикты на выход. Веса поднимаются при первом вызове. |
| `orientation/detectors/ink_axis.py` | Свой детектор: ось текста по сгущению глифов, знак — по выносным элементам. |  |
| `orientation/detectors/ocr_vote.py` | Арбитр: распознавание на каждом ДОПУСТИМОМ повороте, побеждает самый читаемый. |  |
| `orientation/detectors/optional.py` | Сторонний ONNX-классификатор ориентации, подключаемый файлом-описанием. | `OnnxOrientation` — Четырёхклассовый классификатор из ONNX-файла по описанию рядом с ним. |
| `orientation/detectors/osd.py` | Tesseract OSD — штатная детекция ориентации и письменности (``--psm 0``). |  |
| `orientation/detectors/profile.py` | Классический проекционный профиль: ось текста по периодичности строк. |  |
| `orientation/detectors/surya_lines.py` | Ось текста по строкам, найденным детектором Surya. | `SuryaAxis` |
| `orientation/image_io.py` | Чтение полосы в рабочие копии. |  |
| `orientation/pdf_pages.py` | Номер полосы в промежуточном PDF — из базы разметки. | `PdfPage` |
| `orientation/report.py` | Отчёты по прогону: таблицы, CSV и каталог симлинков. | `LinkDirError` (Exception) — В каталоге симлинков нашлось что-то, кроме симлинков. |
| `orientation/validation.py` | Проверка детекторов на синтетических поворотах заведомо прямых полос. | `Trial` — Один поворот одной полосы: что подали и что ответили. |
| `pen_marks.py` | Снятие следов шариковой ручки со скана линейной комбинацией каналов. | `FixResult` — Итог по одному файлу. |
| `rotation.py` | Углы поворота полосы: единые обозначения для базы, CVAT, детекции и очистки. | `RotationError` (ValueError) — Неразборный или недопустимый набор углов. |
| `scan_tree.py` | Обход папки пака: пак -> годовой комплект -> выпуск -> полоса. | `ScannedPage` — Полоса: путь к файлу и его место в выпуске.<br>`ScannedIssue` — Выпуск: папка с полосами.<br>`ScannedYear` — Годовой комплект: папка с выпусками. |
| `table_detection/__init__.py` | Детектор таблиц и блок-схем на полосе: линейки, вид объекта, рамка без разрезанных букв. | `Region` — Находка в координатах ОРИГИНАЛА и в терминах базы: вид из ``TABLE_KINDS`` и JSON. |
| `table_detection/detector.py` | Детектор таблиц и блок-схем: связные рёбра, вид объекта, граница, не режущая букв. |  |
| `table_detection/geometry.py` | Общие типы: рамка, таблица, ячейка, сетка. Плюс арифметика, которую иначе перепишут трижды. | `Box` — Прямоугольник в пикселях; ``x1``/``y1`` — за последним пикселем, как срез питона.<br>`TableBox` — Найденная таблица.<br>`Edge` — Линейка на границе одной полосы: где она проходит и какая она толщины.<br>`Cell` — Клетка сетки. Объединённая записана один раз, в левой верхней клетке.<br>`Grid` — Сетка таблицы: разделители и ячейки. |
| `table_detection/grid.py` | Сетка ячеек по линейкам: разделители, объединения, шапка. |  |
| `table_detection/kind.py` | Вид объекта по линейкам: таблица, схема или рисунок. | `KindFeatures` |
| `table_detection/layout.py` | Разметка surya layout: блоки полосы с видом (Table, Figure, Form, Text…). | `Block`<br>`PageLayout` — Блоки одной полосы в пикселях той копии, что подавалась модели (``width`` × ``height``). |
| `table_detection/quality.py` | Меры качества РАМКИ находки — без разметки, а потому применимые ко всему паку. | `BoxQuality` — Четыре меры одной рамки. Все — в долях или в миллиметрах, не в пикселях. |
| `table_detection/refine.py` | Доводка рамки находки: выбросить чужое, поставить рамку по рёбрам, не разрезать буквы. | `Pushed` |
| `table_detection/rules.py` | Линейки четвёртой версии: цепочки фрагментов и связные ядра вместо склейки дилатацией. | `Fragment` — Кусок линейки: габарит, ось, наклон, площадь краски. |
| `table_detection/ruling.py` | Таблица по линейкам: морфология вместо сети. | `Segment` — Отрезок линейки в координатах рабочей копии.<br>`Lines`<br>`ClusterPolicy` — Пороги кластеризации одним объектом, чтобы третья версия детектора отличалась от второй<br>`Cluster` — Скопление линеек: рамка и ТЕ САМЫЕ отрезки, из которых она получилась. |
| `table_detection/verify.py` | Проверка находки детектора: таблица это или что-то другое. | `Features` — Признаки одной находки. Все безразмерные или в штуках. |
| `tesseract.py` | Общий вызов tesseract для детекторов разметки: PGM во временный файл, запуск, разбор TSV. | `Word` — Одно слово из TSV tesseract: где стоит (пиксели поданной картинки) и что прочитано. |
| `toc/decide.py` | Решение по выпуску: какие полосы окна — оглавление и какого вида. | `Thresholds` — Пороги решения. Значения — по пробному прогону по всему паку-1 (2 091 полоса окна):<br>`Decision` — Решение по полосе: вид (``None`` — не оглавление), сила и почему. |
| `toc/export.py` | Выгрузка найденных полос оглавления в списки для внешнего OCR. | `ExportStats` |
| `toc/features.py` | Признаки одной полосы: метка surya из кэша разметки и текстовые признаки tesseract. | `PageFeatures` — Признаки полосы. Поля surya — нули, если кэша разметки нет; текстовые — нули при<br>`Token` — Слово строки без пунктуации и цифр: в верхнем регистре, плюс как оно было набрано. |
| `toc/labels.py` | Эталонная разметка полос окна: ``rel_path,label[,note]`` с метками contents / index / none. | `Label` |
| `toc/report.py` | Проверка детектора по эталону: матрицы ошибок по признакам и по решению, списки промахов. | `Confusion` |
| `toc/run.py` | Прогон детектора оглавлений по паку: окно полос каждого выпуска -> признаки -> решение -> база. | `TocParams`<br>`IssueResult`<br>`TocStats`<br>`_Job` |
| `toc/sheet.py` | Контактный лист окна выпуска: по нему размечается эталон и разбираются ошибки. |  |
| `validation/cases.py` | Валидационная выборка: папки с примерами дефектов -> что должно получиться. | `Defect` — Тип дефекта: как называется папка, что от детекции ждут и как это проверить.<br>`Case` — Одна полоса выборки: путь в паке, тип дефекта и имя исходного оверлея. |
| `validation/checks.py` | Проверка «починен ли дефект» по найденным на полосе областям. |  |
| `validation/report.py` | Отчёт по валидационному прогону: консоль, markdown и CSV. |  |
| `validation/run.py` | Прогон детекции по валидационной выборке и сравнение с ожиданиями. | `ValidateParams` — Параметры прогона ``validate``.<br>`CaseOutcome` — Итог по одной полосе выборки.<br>`ControlOutcome` — Итог по контрольной полосе: были области в базе — остались ли.<br>`ValidateReport` — Всё, что печатает и пишет отчёт.<br>`_Job` |

## ocr_utils/show_through_detection

Поиск полос, на которых просвечивает текст с оборота листа (show-through).

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: python -m ocr_utils.show_through_detection |  |
| `analysis.py` | Прогон метрик просвечивания по файлам: чтение, деление на полосы, баллы, ранги. | `HalfResult` — Результат замера одной полосы (половины разворота).<br>`FileResult` — Результат анализа одного кадра. |
| `cli.py` | CLI: поиск полос с просвечивающей бумагой в папке со сканами. |  |
| `metrics/base.py` | Общий интерфейс метрик просвечивания бумаги. | `Algorithm` — Описание одной метрики просвечивания. |
| `metrics/gap_abs.py` | Абсолютный отклик в межстрочьях — запасная метрика без опоры на поля. |  |
| `metrics/gap_contrast.py` | Контраст «межстрочья против полей» — основная метрика просвечивания. |  |
| `metrics/ghost_ink.py` | Доля призрака, который переживёт предварительную бинаризацию. |  |
| `metrics/midtone.py` | Доля «полутонового слоя» в межстрочьях — метрика по площади, а не по контрасту. |  |
| `page_split.py` | Деление кадра-разворота на две полосы по корешку. | `Spread` — Найденное деление кадра. |
| `report.py` | Отчёты о просвечивании: консольные таблицы, markdown, CSV, папка симлинков. | `LinkDirError` (Exception) — В целевой папке для симлинков лежит что-то, кроме симлинков. |
| `zones.py` | Нормировка полосы и зоны замера: где межстрочья, где чистые поля, где краска. | `Zones` — Разобранная полоса: карты и маски, общие для всех метрик просвета. |

## ocr_utils/zonal_deblur

Устранение зонального (неравномерного по кадру) смаза на сканах.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: python -m ocr_utils.zonal_deblur. |  |
| `cli.py` | CLI: устранение зонального смаза на скане. |  |
| `deconv.py` | Деконволюция с ядром, плавно меняющимся по кадру. |  |
| `psf.py` | Оценка пространственно-переменного размытия по спектру мощности. | `BlurCell` — Оценка размытия в одной ячейке сетки.<br>`BlurField` — Сетка оценок размытия по всему кадру. |

## research

Исследовательские пакеты: то, что ещё проверяется, а не работает в конвейере.

| Модуль | Назначение | Классы |
|---|---|---|

## research/deepseek_damage_lesson

Учебный скрипт: одна повреждённая страница → DeepSeek V4.1 Flash → JSON и Markdown с пометками.

| Модуль | Назначение | Классы |
|---|---|---|
| `compare_prompt_languages.py` | Сравнение английских и русских промптов на мини-наборе повреждённых сканов. |  |
| `ocr_damaged_page.py` | Одна повреждённая страница → DeepSeek V4.1 Flash через OpenRouter → JSON, Markdown и метаданные. |  |

## research/external_ocr_models

Внешние OCR-модели: страница журнала → размеченный markdown через VLM.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``uv run python -m research.external_ocr_models <команда>``. |  |
| `cli.py` | Команды пакета: ``uv run python -m research.external_ocr_models <команда>``. |  |
| `client.py` | Клиент OpenRouter переехал в боевой пакет ``ocr_utils.external_ocr_services.client``; здесь — реэкспорт. |  |
| `evaluate.py` | Метрики по выходам моделей: буквы, структура, надёжность, цена. | `Structure`<br>`PageOutput`<br>`PageScore` |
| `imaging.py` | Подготовка полосы к отправке: уменьшить, обесцветить, сжать, закодировать. | `PreparedImage` |
| `local/deepseek_ocr2/worker.py` | DeepSeek-OCR-2 по папке полос: «Convert the document to markdown» → markdown. |  |
| `local/dots_ocr/worker.py` | dots.ocr по папке полос: layout + текст одной моделью → markdown и поля страницы. |  |
| `local/marker/worker.py` | Marker по папке полос: layout + OCR surya → markdown с заголовками и таблицами. |  |
| `local/paddleocr_vl/worker.py` | PaddleOCR-VL 1.6 по папке полос: PP-DocLayout + VLM → markdown (таблицы в HTML). |  |
| `local.py` | Локальные специализированные OCR-движки: запуск воркера изолированного окружения. |  |
| `models.py` | Реестр моделей: что гоняем, под каким именем и с какими особенностями запроса. | `ModelSpec` |
| `ocr.py` | Одна полоса → один запрос к модели → PageResult и .meta.json. | `RunOptions` |
| `render.py` | Разложить PageResult в .md: YAML-шапка с полями страницы и тело в markdown. |  |
| `report.py` | Сводные таблицы по оценкам: по моделям и по полосам. Только читает готовые выходы. |  |
| `schema.py` | Ответ модели на одну полосу: поля, JSON-схема и терпимый разбор. | `ParseError` (ValueError) — Ответ модели не удалось привести к PageResult.<br>`PageResult` |
| `spreads.py` | Разрезка фото-разворотов на страницы по прямой линии сгиба. | `Fold` — Прямая сгиба в координатах исходного кадра: ``x = slope * y + intercept``. |

## research/geometry_regression

Страницы, на которых коррекция геометрии FineReader сделала хуже, чем было.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``uv run python -m research.geometry_regression <команда>``. |  |
| `cli.py` | Команды пакета: ``uv run python -m research.geometry_regression <команда>``. |  |
| `edges.py` | Кромки колонок: наклон левого и правого края выключенного текста. | `Edge` |
| `field.py` | Поле смещений «без коррекции → с коррекцией»: что FineReader сделал со страницей. | `Field` — Поле смещений страницы B → A на копии 150 dpi. |
| `lines.py` | Строки текста попарно: наклон и волнистость каждой строки «было \| стало». |  |
| `metrics.py` | Все измерения одной пары страниц в один плоский словарь. | `Params` — Размеры, привязанные к бумаге. Умолчания — для пака-1 (журнальная полоса ~170×260 мм).<br>`PageMeasure` |
| `overlay.py` | Картинка «было \| стало» для просмотра находок глазами. |  |
| `regions.py` | Области страницы на рендере без коррекции: line art и строки текста. | `TextLine` — Строка текста на рабочей копии с аппроксимацией её центр-линии. |
| `render.py` | Пары PDF «с коррекцией \| без» и рендер их страниц. | `PdfPair` — Один выпуск в обоих прогонах. |
| `report.py` | CSV с метриками по страницам и markdown-сводка прогона. | `PageRow` — Одна страница в CSV: метрики плюс вердикт. |
| `scoring.py` | Пороги на разности и сводный балл страницы. | `Verdict`<br>`Thresholds` — Пороги порчи и выигрыша с перекрытиями вида ``имя=число`` из командной строки. |
| `stretch.py` | Строки по одним и тем же глифам: наклон, волна и вертикальное растяжение куска за куском. | `Chunk` |
| `strokes.py` | Длинные прямые штрихи (LSD): наклон к оси, поворот и параллельность попарно «было \| стало». | `Stroke` |
| `vlm.py` | Пробник: может ли VLM (DeepSeek V4.1 Flash через OpenRouter) сама увидеть порчу геометрии. | `Probe` |

## research/legacy

| Модуль | Назначение | Классы |
|---|---|---|

## research/legacy/table_processing

Таблицы с повёрнутым текстом: найти, прочитать, подменить прямым.

| Модуль | Назначение | Классы |
|---|---|---|
| `__main__.py` | Точка входа: ``uv run python -m research.legacy.table_processing <команда>``. |  |
| `cache.py` | Дисковый кэш результатов: тонкая обёртка над ``curved_lines.cache.PageCache``. | `ResultCache` |
| `check.py` | Сверка детектора таблиц: старая версия, новая и то, что нашёл сам FineReader. | `DocxTableRow` — Одна таблица из выгрузки DOCX и страница, на которую она легла.<br>`FindingRow` — Одна находка детектора со всеми признаками и вердиктом новой ступени.<br>`PageRow` — Полоса, на которой есть хоть что-то: таблица в DOCX или находка детектора.<br>`Finding` — Одна находка кластеризации с признаками и вердиктом проверки. |
| `cli.py` | Команды пакета: ``uv run python -m research.legacy.table_processing <команда>``. |  |
| `detection/base.py` | Общий контракт детекторов таблиц. | `BatchTableDetector` (Protocol) — GPU-детектор: модель грузится лениво, на вход пачка серых полос.<br>`TableDetector` |
| `detection/kind.py` | Вид объекта по линейкам: таблица, схема или рисунок. |  |
| `detection/quality.py` | Меры качества рамки и компоненты краски под границей. |  |
| `detection/refine.py` | Доводка рамки находки третьей версии: обрезка по линейкам и прилипание к просвету. | `Snapped` |
| `detection/rules_v4.py` | Линейки четвёртой версии: цепочки фрагментов и связные ядра. |  |
| `detection/ruling.py` | Таблица по линейкам: морфология вместо сети (первая версия детектора). |  |
| `detection/ruling_v3.py` | Детектор таблиц, третья версия: те же линейки, но рамка доводится до ума. |  |
| `detection/ruling_v4.py` | Детектор четвёртой версии (v4.2): связные рёбра, вид объекта, граница без разрезанных букв. |  |
| `detection/verify.py` | Проверка находки детектора: таблица это или что-то другое. |  |
| `geometry.py` | Общие типы стенда: рамка, таблица, ячейка, сетка. |  |
| `imaging.py` | Чтение полос и вырезание кусков — в одном месте, потому что дорого и легко испортить. |  |
| `labels.py` | Ручная разметка: то, по чему сравниваются алгоритмы. | `TableLabel`<br>`DetectorLabel` — Одна находка детектора с вердиктом человека — то, на чём калибруется ``detection.verify``.<br>`DetectorPageLabel` — Диагноз человека по ОДНОЙ ПОЛОСЕ, а не по одной находке.<br>`CellLabel`<br>`TextLabel` |
| `layout/surya.py` | Surya layout для стенда: блоки полосы с видом и их кэш на диске. | `Predictor` — Ленивая обёртка над ``surya.layout.LayoutPredictor``: модель грузится при первом вызове. |
| `mining/anchor.py` | Где на полосе стоит таблица: по словам текстового слоя. | `Word` |
| `mining/broken.py` | Таблицы, которые FineReader не увидел: есть на скане, нет в DOCX. | `BrokenTable` — Находка на странице, где в DOCX таблицы нет. |
| `mining/docx_tables.py` | Таблицы выгрузки FineReader: чтение DOCX в порядке документа. | `DocxTable` — Одна таблица выгрузки вместе с окрестностями. |
| `mining/export.py` | Добыча примеров: от DOCX до вырезанной таблицы на диске. | `MinedTable` — Строка манифеста: одна таблица со всеми числами, по которым её нашли. |
| `mining/garbage.py` | Счёт «мешанины»: какие таблицы выгрузки FineReader испортил. | `GarbageScore` — Разложенный счёт: в отчёте нужен не только итог, но и из чего он сложился. |
| `mining/page_match.py` | Какой странице PDF соответствует таблица DOCX. | `PageText`<br>`PageMatch` |
| `ocr/base.py` | Общий контракт распознавателей: выпрямленная ячейка → строки текста. | `OcrResult` — Что прочитал движок в одной ячейке.<br>`Recognizer` (Protocol)<br>`Engine` |
| `ocr/evaluate.py` | Сравнение распознавателей: расстояние Левенштейна на символах. | `EngineScore` — Счёт одного распознавателя по размеченным ячейкам. |
| `ocr/paddle_engine.py` | Распознавание ячейки через PaddleOCR в ОТДЕЛЬНОМ окружении. | `PaddleRecognizer` |
| `ocr/surya_engine.py` | Распознавание ячейки через surya: детектор строк плюс распознаватель. | `SuryaRecognizer` |
| `ocr/tesseract_engine.py` | Распознавание ячейки через tesseract (подпроцессом, как везде в проекте). | `TesseractRecognizer` |
| `paddle_env/worker.py` | Распознавание вырезок ячеек через PaddleOCR. Запускается в отдельном окружении. |  |
| `pages.py` | Связь «таблица в DOCX → страница PDF → файл скана». | `ScanPage` — Полоса выпуска: чем она была в PDF и где лежит её заострённая копия. |
| `paths.py` | Пути пака-1 и умолчания прогона. |  |
| `pipeline.py` | Сквозной прогон по одной вырезанной таблице: сетка → боковые ячейки → текст → замена. | `CellOutcome` — Что случилось с одной ячейкой.<br>`TableOutcome` — Что случилось с одной таблицей. |
| `render/compose.py` | Сборка «стало»: таблица, в которой боковой текст заменён прямым. | `Replacement` — Что и на что меняем в одной ячейке.<br>`Composed` — Результат сборки со следами того, что пришлось сделать. |
| `render/fonts.py` | Шрифт замены и укладка текста в ячейку. | `Fit` — Уложенный текст: каким кеглем, какими строками и сколько места занял. |
| `render/sheet.py` | Листы «было — стало»: то, ради чего всё и затевалось. |  |
| `report.py` | Отчёты: CSV, markdown и контактные листы. |  |
| `rotation/base.py` | Общий контракт детекторов поворота ЯЧЕЙКИ. | `CellCrop` — Вырезанная ячейка со всем, что нужно детектору.<br>`BatchCellDetector` (Protocol) — GPU-детектор: модель грузится лениво, на вход пачка вырезанных ячеек.<br>`CellDetector` |
| `rotation/combine.py` | Сведение вердиктов детекторов в один ответ по ячейке и по таблице. | `CellDecision` — Итог по одной ячейке со следами того, кто как проголосовал. |
| `rotation/doctr_crop.py` | Классификатор ориентации ВЫРЕЗКИ из docTR (``mobilenet_v3_small_crop_orientation``). | `DoctrCellOrientation` (BatchCellDetector) |
| `rotation/evaluate.py` | Сравнение детекторов поворота ячейки по ручной разметке. | `Score` — Счёт одного детектора. |
| `rotation/glyph_aspect.py` | Ось текста в ячейке по форме самих букв. |  |
| `rotation/ink_axis_cell.py` | Ось и сторона поворота ячейки: смыкание глифов и выносные элементы. |  |
| `rotation/ocr_vote_cell.py` | Арбитр стороны: распознавание ячейки на каждом допустимом повороте. |  |
| `rotation/profile_cell.py` | Ось текста в ячейке по периодичности проекционного профиля. |  |
| `rotation/surya_axis_cell.py` | Ось текста в ячейке по строкам, найденным детектором Surya. | `SuryaCellAxis` (BatchCellDetector) |
| `structure/base.py` | Общий контракт разборщиков структуры: вырезанная таблица → сетка ячеек. | `BatchCellExtractor` (Protocol)<br>`CellExtractor` |
| `structure/ruling_grid.py` | Сетка ячеек по линейкам: разделители, объединения, шапка. |  |
| `warping/diagnose.py` | Кто искорёжил таблицу: типография, скан или коррекция геометрии FineReader. | `Diagnosis` — Одна таблица в трёх источниках. |
| `warping/engines/base.py` | Общий контракт выпрямителей таблицы. | `Warped` — Результат выпрямления: картинка, поле смещений и слово о том, что вышло.<br>`Warper` — Запись в реестре выпрямителей. |
| `warping/engines/deskew.py` | Поворот на медианный угол линеек — точка отсчёта для всех остальных. |  |
| `warping/engines/perspective.py` | Гомография по четырём углам таблицы: лечит трапецию. |  |
| `warping/engines/rules_separable.py` | Раздельное поле смещений: вертикальное по горизонтальным линейкам, горизонтальное — по вертикальным. |  |
| `warping/engines/rules_tps.py` | Тонкая пластина по пересечениям линеек: самый гибкий и самый ломкий способ. |  |
| `warping/engines/textline_engine.py` | Готовый выпрямитель проекта по СТРОКАМ ТЕКСТА, применённый к вырезке таблицы. |  |
| `warping/evaluate.py` | Сравнение выпрямителей: стало ли ровнее и не испортилось ли изображение. | `Outcome` — Что вышло у одного выпрямителя на одной таблице. |
| `warping/metrics.py` | Меры геометрии таблицы: кривизна линеек и регулярность решётки. | `Geometry` — Геометрия одной таблицы. Пустые поля означают «мерить было не по чему». |
| `warping/trace.py` | Линейка как ломаная: для каждого x — свой y. | `Polyline` — Прослеженная линейка в координатах поданного изображения. |
