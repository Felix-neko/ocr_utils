# Сравнение алгоритмов детекции расфокуса на подшивках «Социалистической индустрии»

**Дата:** 2026-08-26
**Код:** `validate_defocus_batches.py`, метрики — `ocr_utils/defocus_detection/metrics/`
**Данные:** 4008 кадров RAF (Fujifilm X100VI, превью 4416×2944), 13 подшивок, 1985–1988.
**Разметка:** 176 кадров помечены вручную как брак, 336 — проверенно хорошие.
**Полные таблицы:** `defocus_calibration/si_metrics_tables.md`, сырые признаки —
`defocus_calibration/si_features2.csv`.

---

## 1. Итог

**Победила `dom` — метрика Kumar-Chen-Doermann (ICPR 2012), добавленная по итогам обзора
готовых решений.** Она выиграла по всем трём независимым критериям сразу, чего не сделал
никто другой:

| критерий | `dom__best` | `edge_width__worst` (работает сейчас) |
|---|--:|--:|
| точность ранжирования (AP на чистом разрезе 1987) | **0.732** | 0.330 |
| устойчивость уровня между съёмочными сессиями | **2.6 %** | 12.9 % |
| связь с яркостью внутри сессии | **+0.058** | −0.018 |

Разрыв по точности — **в 2.2 раза**. Это не тонкая настройка, а смена рабочей метрики.

**Пороги для неё объективны и не требуют подстройки под съёмку.** Один и тот же порог даёт
ту же полноту, что и персонально настроенный под каждую сессию, и при этом сам сжимает
список на просмотр с 10 % в плохую съёмку до 1 % в хорошую. Готовые значения и цена
разных уровней чувствительности — раздел 5.

Три вещи, которые стоит знать про этот вывод:

* **Проверка DeQA-Doc (7 млрд параметров, GPU) закончилась не в её пользу**: AUC 0.68–0.77
  против 1.00 у `dom` на тех же пикселях. Разбор — в разделе 7.
* **Детектор смаза на реальных данных не подтвердился**, хотя на синтетике работал
  безупречно. Раздел 6.
* **По дороге я дважды сделал неверные выводы** и оба раза их опроверг замером. Это
  описано в разделе 8, потому что ошибки здесь поучительнее результатов.

---

## 2. Как мерили и почему именно так

**Ранжирование считается ВНУТРИ подшивки, а не по всей выборке.** Это не педантизм:
при сквозном счёте по всем 13 подшивкам наверх вылезает `laplacian` — наивная база,
про которую в самом пакете написано, что брать её нельзя из-за зависимости от контраста
и ISO. Причина в том, что в общей куче выигрывает метрика, хорошо разделяющая **съёмочные
сессии** (у них разная экспозиция), а не расфокус. Инструментом же пользуются иначе:
ранжируют кадры внутри одной папки.

**Решающая величина — средняя точность (AP), а не AUC.** Доля брака в выборке 4.4 %,
и при таком дисбалансе AUC выглядит благополучно даже у посредственного детектора:
почти все варианты дают 0.92–0.99, и различить их по этому числу нельзя. AP реагирует
на то, сколько хороших кадров набилось в верхушку списка, — то есть ровно на то, что
чувствует человек, просматривающий отчёт.

**Главный разрез — 1987.** В подшивках 1985 забракованные кадры сняты в июле, а часть
хороших — пересъёмки августа, то есть в другую сессию с другим освещением: контраст-
зависимая метрика разделит их даром, не глядя на фокус. В 1987 брак и хорошие кадры
сняты в одну сессию, и разметки там больше всего — 116 кадров из 176.

---

## 3. Точность ранжирования

Средняя точность по четырём подшивкам 1987, взвешенная по числу брака:

| вариант | AP |
|---|--:|
| **`dom__best`** | **0.732** |
| `laplacian__best` | 0.728 |
| `dom_eq__best` | 0.728 |
| `laplacian__median` | 0.719 |
| `dom__median` | 0.705 |
| `dom_eq__median` | 0.699 |
| `dom__worst` | 0.667 |
| `hf_mid__best` | 0.661 |
| `edge_width__worst` | 0.330 |

По подшивкам поимённо видно, что превосходство `dom` не случайно:

| подшивка | брака | `edge_width__worst` | `dom__best` |
|---|--:|--:|--:|
| 1987/01-03 | 35 | 0.413 | **0.625** |
| 1987/04-06 | 21 | 0.239 | **0.884** |
| 1987/07-09 | 37 | 0.352 | **0.721** |
| 1987/10-12 | 23 | 0.250 | **0.775** |

Практический смысл: при просмотре худших 10 % подшивки `dom__best` показывает **73 %**
всего брака, `edge_width` — около половины.

Оговорка про `laplacian`. Он идёт вторым и по AP почти неотличим от `dom`, но брать его
нельзя: уровень метрики скачет между сессиями (см. следующий раздел), а внутри сессии он
сильнее прочих связан с яркостью (+0.103 против +0.058 у `dom`). Хорошее место в этой
таблице он занимает благодаря тому, что 1987 снят в две близкие по условиям сессии.

---

## 4. Устойчивость уровня между сессиями — то, от чего зависят пороги

Для абсолютного порога мало быть точным внутри папки: уровень метрики обязан не «плыть»
от съёмки к съёмке. Медианы по 11 съёмочным сессиям:

| метрика | размах медиан относительно медианы |
|---|--:|
| **`dom__best`** | **2.6 %** |
| `edge_width__worst__norm` | 10.9 % |
| `edge_width__worst` | 12.9 % |
| `cpbd__worst` | 19.0 % |

`dom` не только точнее — он ещё и на порядок стабильнее по уровню. И его медиана честно
отражает качество съёмки: две худшие сессии (13.08 и 17.08, по 46–47 бракованных кадров)
дали самые низкие медианы (3.035 и 3.060), а лучшие (18.07 с одним браком и 19.08 без
брака вовсе) — самые высокие (3.115 и 3.087).

Это и есть готовый фундамент для абсолютных порогов: величина, которая одинаково
означает одно и то же в разные дни.

Отдельно про `cpbd`: 19 % разброса — худший результат, и это ожидаемо. Метрика быстро
насыщается (на синтетике падает с 1.00 до 0.06 уже при σ = 0.6), поэтому её рабочий
диапазон узкий и уползает от условий съёмки.

---

## 5. Абсолютные пороги: они существуют и не требуют настройки под сессию

Главный практический вопрос — можно ли задать пороги раз и навсегда или их придётся
подбирать под каждую съёмку. **Ответ: раз и навсегда.**

### Доказательство — общий порог против персонального

Для каждой сессии взят общий порог 2.970 и персональный, настроенный по этой же сессии
так, чтобы он пометил ровно столько же кадров. Полнота совпала до процента везде:

| сессия | общий порог | персональный |
|---|--:|--:|
| 18.07 | 100 % | 100 % |
| 20.07 | 26 % | 26 % |
| 21.07 | 0 % | 0 % |
| 22.07 | 60 % | 60 % |
| 07.08 | 60 % | 60 % |
| 10.08 | 22 % | 22 % |
| 11.08 | 38 % | 38 % |
| 13.08 | 54 % | 54 % |
| 17.08 | 68 % | 68 % |
| 18.08 | 43 % | 43 % |

Персональная настройка не даёт **ничего**: уровень `dom` между сессиями не плывёт
(раздел 4), поэтому подстраивать нечего.

### Порог сам подстраивает длину списка под качество съёмки

Это то, ради чего всё затевалось. Порог один и тот же, а доля затегированных кадров
воспроизводит фактическую долю брака в сессии:

| сессия | брака фактически | помечено порогом 2.970 |
|---|--:|--:|
| 19.08 (1988, отсмотрена целиком, чистая) | 0.0 % | **0.3 %** |
| 07.08 | 1.3 % | **1.3 %** |
| 22.07 | 2.5 % | **2.5 %** |
| 11.08 | 2.8 % | 2.5 % |
| 18.08 | 6.8 % | 3.5 % |
| 13.08 | 10.1 % | **9.6 %** |
| 17.08 | 10.2 % | **9.3 %** |

Список на просмотр сам сжимается до процента в удачную съёмку и разжимается до десяти
в неудачную. Отбор по фиксированной доле худших этого не может по построению — именно
он и заставлял просматривать 15 % там, где хватило бы одного.

### Почему это работает: распределения почти не перекрываются

| класс | p10 | медиана | p90 |
|---|--:|--:|--:|
| тяжёлый расфокус (15) | 2.807 | 2.944 | 3.038 |
| средний (61) | 2.895 | 2.961 | 3.042 |
| лёгкий (40) | 2.956 | 2.996 | 3.037 |
| `.blurry` (60) | 2.901 | 2.963 | 3.019 |
| **не помечен (3832)** | **3.027** | **3.087** | 3.123 |
| **1988, отсмотрена и чиста (304)** | **3.058** | **3.087** | 3.118 |

Показательно последнее: у проверенно чистой подшивки распределение практически совпадает
с общей массой неразмеченных кадров. Значит шкала действительно абсолютная, а не
подогнана под конкретную съёмку.

### Готовые пороги

| тег | условие | кадров | из них помечены вручную |
|---|---|--:|--:|
| `defocus_heavy` | `dom__best < 2.92` | 42 | 34 (81 %) |
| `defocus_medium` | `dom__best < 2.96` | 62 | 35 (56 %) |
| `defocus_light` | `dom__best < 3.00` | 187 | 59 (32 %) |

Суммарно помечается 291 кадр из 4008 (**7.3 %**), ловится **73 % размеченного брака**,
на проверенно чистой подшивке 1988 — **2 ложных срабатывания из 304 (0.7 %)**.

### Чем платить за другую чувствительность

| верхний порог | ловит брака | помечено всего | ложных на чистой 1988 |
|---|--:|--:|--:|
| 2.96 | 39 % | 2.6 % | 0 из 304 |
| 2.98 | 60 % | 4.5 % | 2 из 304 |
| **3.00** | **73 %** | **7.3 %** | **2 из 304** |
| 3.02 | 82 % | 11.5 % | 6 из 304 |

3.00 выбран как точка, после которой список растёт быстрее, чем улов: шаг до 3.02 добавляет
девять процентных пунктов полноты ценой утроения ложных срабатываний.

### Что абсолютным НЕ становится

Полнота по отдельным сессиям гуляет от 0 % до 100 %, и это не дефект порога. В разные дни
вы помечали брак разной тяжести: там, где размечен только `ultralight` на грани
различимости, порог его закономерно не берёт. Абсолютным получается **уровень размытия**,
а не чувствительность к субъективному критерию конкретного дня.

---

## 6. Смаз против промаха фокуса

Результат отрицательный, и его надо назвать прямо.

На синтетике `edge_dir` разделял явления безупречно:

| случай | анизотропия |
|---|--:|
| резкий кадр | 3.15 |
| расфокус σ=1 / σ=2 | 3.18 / 2.65 |
| смаз 7 px | 30.9 |
| смаз 15 px | 83.3 |

На реальных данных — не разделяет:

| группа | медиана анизотропии |
|---|--:|
| помечено смазом (6 кадров) | 2.72 |
| прочий брак | 2.44 |
| неразмеченные | 2.51 |

Шесть примеров — слишком мало для приговора метрике, но и утверждать, что смаз
детектируется, нельзя. Вероятнее всего, реальный смаз от дрожания рук на выдержке
слишком слаб и не прямолинеен, тогда как синтетика моделировала ровный сдвиг на
7–15 пикселей. Чтобы закрыть вопрос, нужна either целенаправленная разметка смаза,
either съёмка контрольной серии с заведомым сдвигом.

---

## 7. Готовая GPU-модель: DeQA-Doc

Проверена **DeQA-Doc-Sharpness** (mPLUG-Owl2, 7.2 млрд параметров, победитель
VQualA 2025 DIQA) на 30 кадрах с известными метками.

**На карту помещается — но только квантованной.** В fp16 это 14.4 ГБ при 14.3 ГБ
свободных. Проверены три формата:

| формат | VRAM | с/кадр | AUC |
|---|--:|--:|--:|
| int8 (bitsandbytes) | 8.03 ГБ | 5.6 | 0.714 |
| NF4 (bitsandbytes) | ~4 ГБ | 5.5 | 0.746 |
| FP8 e4m3 (torchao, нативный на Blackwell) | 7.92 ГБ | 4.1 | 0.730 |

**Квантизация ни при чём.** Три принципиально разных механизма дают один результат в
пределах 0.71–0.75; расхождение между форматами (0.03–0.08) меньше полезного разрыва
«хорошие минус брак» (0.07–0.11). Списать слабость на сжатие нельзя.

**Дело и не в разрешении.** Модель ужимает вход до 448×448, и для целого кадра это
убийственно (сжатие в 9.9 раза, строка петита превращается в 2–3 пикселя). Но кадры
подавались и нативными кропами 448×448, где текст виден в натуральную величину. На тех
же самых пикселях:

| измеритель | AUC |
|---|--:|
| **`dom`** | **1.000** |
| `edge_width` | 0.714 |
| `reblur` | 0.619 |
| `cpbd` | 0.524 |
| **DeQA-Doc** | **0.68–0.77** |

Информация в окне есть и извлекается полусотней строк на процессоре.

**Симптом, объясняющий причину.** Все 30 кадров — от откровенного брака до эталона —
уместились у модели в диапазон 3.0–3.6 из возможных 1–5. Она считает любую газетную
полосу «средне-хорошей» и на фокус почти не реагирует. Кадр, помеченный `_cool_focus`
как образец удачного фокуса, получил 3.35 — неотличимо от тяжёлого расфокуса (3.30).
Это внераспределённость: модель обучена на другом материале и на другом вопросе.

**Вердикт: не брать.** Не потому, что не влезла или медленная, а потому что проигрывает
метрике, которая считается за секунду на процессоре.

Техническая заметка: авторский код написан под `transformers 4.36.1`, у нас 4.57.6, и
между ними сменились сигнатуры в LLaMA-слое. Заглушки ставить нельзя — импорт бы прошёл,
а модель считала бы молча неверно. Прогон делался в изолированном окружении
(`transformers==4.36.2`), проектное не тронуто.

---

## 8. Что я делал неправильно по дороге

Три ошибки, каждая была поймана замером и каждая меняла вывод.

**Гипотеза «`dom` зависит от освещения» — неверна.** Сквозная корреляция с яркостью
+0.32 выглядела приговором для абсолютных порогов, и я так и написал в промежуточном
выводе. Но внутри одной сессии она падает до **+0.058** (превышает 0.2 лишь в 1 сессии
из 11). То есть связь межсессионная и наведённая: разные дни — разная экспозиция и
разное качество фокуса одновременно. Метрика чиста.

**Выравнивание уровней (`dom_eq`) не дало ничего — и не могло.** Идея была снять
зависимость от яркости, растянув уровни по центральным 75 % кадра. Результат: AP 0.728
против 0.732, корреляция +0.333 против +0.319 — нулевой эффект. Задним числом это
очевидно: `dom` есть отношение двух сумм модулей разностей, и при линейном растяжении
шкалы числитель и знаменатель умножаются на один и тот же множитель. Я упустил это,
когда предлагал реализацию. Метрика оставлена в реестре как документированный
отрицательный результат.

**Первая версия `edge_dir` была выброшена целиком.** Она мерила ширину края в четырёх
повёрнутых копиях кадра, и провалилась дважды: поворот сам размывает тем сильнее, чем
дальше угол от нуля (резкий кадр получал «анизотропию» 1.40), а когда я поднял отсечку
длины перепада, чтобы вообще увидеть смаз, в замер хлынули фоновые градиенты и
направленная чувствительность исчезла вовсе. Спектральная версия свободна от обоих:
она не двигает пиксели и не опирается на пороги «что считать штрихом».

Плюс две методические ловушки в самом замере: сквозной счёт по подшивкам (раздел 2) и
вызов `edge_width`/`cpbd` на голом кропе без контекста кадра — обе давали правдоподобные,
но неверные числа.

---

## 9. Что делать дальше

1. **Сменить рабочую метрику на `dom` с агрегацией `best`.** Основание — разделы 3 и 4.
2. **Зашить готовые пороги из раздела 5** (2.92 / 2.96 / 3.00) как пресет: они проверены
   на 11 съёмочных сессиях и на отдельной чистой подшивке. Прежний план предполагал строить
   пороги на `edge_width` и подлежит правке.
3. **Проверить `dom` на нормировку по шагу строк** — нужна ли она вообще, раз уровень и
   так стабилен. Нормировка нужна для переноса на другие газеты (иной кегль и формат),
   а не для порога внутри одного проекта.
4. **Смаз оставить открытым вопросом** до целенаправленной разметки.
5. **Не тащить в проект ни DeQA-Doc, ни pyiqa.** Первая проверена и проиграла; вторую
   имеет смысл замерить тем же скриптом, чтобы закрыть вопрос числом, но ожидания низкие
   по той же причине — обучена на натурных фото.
