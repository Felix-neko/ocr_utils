# page_layout: переезд разбора структуры страницы и A/B

## Задача

Собрать разбор структуры страницы (растр, таблицы, line art, повёрнутый текст, ориентация,
surya layout) в один подпакет `ocr_utils/page_layout` с одними детекторами и одними версиями
для этапа `detect` (разметка в базу и CVAT), детектора порчи геометрии (выбор страницы при
сборке финальных PDF) и правки текстового слоя — и убедиться, что качество этих двух
подсистем не ухудшилось. Повод: до переезда `detect` размечал полосу одним набором
детекторов, а сборка PDF решала судьбу страницы другим (пять конструкторов surya, два формата
кэша, три детектора line art); в CVAT появились дублирующие метки «(крупный штрих)», а line art
выставлялся внутри фотографий и поверх таблиц (пак-2, 2026-09-21).

## Стенд и данные

* Пак-1: 12 135 полос, кэш surya по сканам перенесён из старых pickle (`legacy`); эталоны —
  70 страниц геометрии (`research/geometry_regression/validation/pack1`), 221 ручная область
  `line_art_schema` на 207 страницах + 200 контрольных (`text_layer_fix eval-lineart`),
  190 полос детектора таблиц, выборка текстового слоя 1050 страниц (seed 7, 200 контрольных).
* Пак-2: 597 полос, база `pack2.sqlite`, проект CVAT пересоздан.
* Кэш surya: `/mnt/system/raw/mts/pack1_page_layout/{scan,sharpened,fr_nogeo,fr_geo}`,
  `/mnt/system/raw/mts/pack2_page_layout/scan`; страницы PDF — ~0.7 с GPU на страницу
  (при двух моделях на одном GPU ~1.2 стр/с), `--jobs 12`.
* Код: `46322b3` (ветка `zonal_defocus_with_surya`), 2026-09-21.

## Метод

`ocr_utils/page_layout/README.md`. Коротко: `PageImage` (варианты картинки, кадр surya 150 dpi
≤ 2048 px), единственная `SuryaLayoutModel`, JSON-кэш по варианту и отпечатку источника,
фасад `PageLayout` с порядком растр → таблицы → line art вне них → повёрнутый текст вне таблиц
→ ориентация; два этапа под пул (surya только в родителе, промах в воркере — ошибка,
кэш набивает `prefill-surya`). Единый детектор line art: затравки — схемы/рисунки детектора
таблиц, блоки surya Figure/Form/Picture (Picture — если его не подтвердил растр), связные пятна
и скопления линеек (`line_art.features`); все через одну пиксельную проверку, исключения —
растр и таблицы; уверенность = доля семейств источников (⅓, ⅔, 1). Версии семейств:
растр 10 (алгоритм не менялся), таблицы 1 (не менялся), line art 2, повёрнутый текст 1,
ориентация 1.

## Результат

### Line art, эталон 221 области (no-geo PDF, `eval-lineart`, `pack1_v2/lineart_eval.csv`)

| источник | предск. | совп. | точн. | полн. | накр. | F1 | ложн. на 200 контр. |
|---|---|---|---|---|---|---|---|
| figures (FineReader) | 297 | 183 | 0.62 | 0.82 | 0.86 | 0.70 | 12 |
| detector (таблицы, схема/рисунок) | 146 | 143 | 0.98 | 0.65 | 0.65 | 0.78 | 0 |
| ink (пятна, `line_art_detection`) | 342 | 187 | 0.55 | 0.85 | 0.94 | 0.66 | 25 |
| surya по сканам (кэш scan) | 233 | 202 | 0.87 | 0.91 | 0.95 | 0.89 | 0 |
| surya по рендеру no-geo (новое) | 239 | 203 | 0.85 | 0.92 | 0.93 | 0.88 | 15 |
| **page_layout** (новое) | 250 | 211 | 0.84 | 0.95 | 0.97 | **0.90** | 15 |
| union четырёх (прежний лучший) | 276 | 217 | 0.79 | 0.98 | 1.00 | 0.87 | 25 |

Surya по бинарным рендерам FineReader (риск из плана) работает не хуже surya по сканам
(F1 0.88 против 0.89). Ложные `page_layout` на контроле — рамки и логотипы рубрик, шапки в
рамке (те же, что у `ink`); фотографии исключены известным растром из базы (как на `detect`).

### Геометрия, эталон 70 страниц (`regress`, v12 → v13)

`regression_v12_v13.md`: **63 из 70 против 64**, одна смена вердикта — `full_1975_07 с.44`
(метка good; было `ok 0.36 line`, стало `bad 2.61 bend`): детектор таблиц нашёл на странице
таблицу, и её правая линейка после коррекции FineReader получила изгиб у верха (~2.6 мм),
которого в v12 не мерили, потому что таблица не была рамкой line art. Картинка «было | стало»:
`~/Projects/mts_markup/pack1_page_layout_review/pair_1975_07_44.jpg` — решение за пользователем
(изгиб виден; вопрос, считать ли его порчей при в целом хорошей коррекции).

### Таблицы, 190 полос (`run_compare_detector.sh`)

Без изменений: ждём таблицу — нашёл 82/84, ждём пусто — таблиц 22/102, компонент под границей 9
(алгоритм переехал без правок).

### Растр

Папка эталонов `некоторые проблемные картинки` на диске отсутствует — `validate` не
воспроизводим; алгоритм не менялся (те же модули, версия 10), 19 юнит-тестов растра прошли.

### Текстовый слой, выборка 1050 страниц (`pack1_v2`)

Та же выборка (seed 7), те же три стадии (`run_sample.sh` → `run_second_opinion.sh` → `run_fix.sh`):

| | v1 (детектор таблиц без surya, docstrum) | v2 (page_layout, surya fr_geo) |
|---|---|---|
| зон | 4763 | 4700 (−1.3 %) |
| удалено слов (blanked) | 9043 | 8974 (−0.8 %) |
| вставок | 2750 | 2807 (+2.1 %) |
| усечено (trimmed) | 9 | 14 |
| сверка после правки | 0 расхождений, 1050/1050 ok | 0 расхождений, 1050/1050 ok |
| 200 контрольных | 0 удалений | 0 удалений |
| таблиц / областей line art на выборке | 612 / 156 | 588 / 458 |
| зон: подписи на line art / прямые без слоя в ячейках | 462 / 456 | 568 / 219 |

Line art на выборке втрое больше (полнота единого детектора 0.95 против 0.65 у одного
детектора таблиц), подписей на схемах найдено на 23 % больше. 23 страницы потеряли таблицу —
детектор таблиц с surya перевёл её в схему (блок Figure/Picture над затравкой): на них ушли
237 «прямых ячеек без слоя», но вставок это стоило только 3 (в v1 эти ячейки почти не читались).
Первый прогон без второй стадии давал вставок 2630 (−4.4 %): разница целиком в surya-OCR второго
мнения, не в детекторах.

### detect, пак-2 (597 полос)

Растр и таблицы не пересчитывались (версии те же): color 6, grayscale 11, stamp_suspect 3,
table 32 — равны снимку до переезда. Line art: 63 (было 4 `line_art_schema` + 91 `stroke_*`),
ни одна рамка не пересекает растр/таблицу больше чем на 20 % площади (SQL по базе);
повёрнутый текст: 18. Проект CVAT «пак-2» пересоздан: 133 шейпа, 12 тегов, 17 меток без
«(крупный штрих)», с «Повёрнутый текст».

## Что пробовали и отвергли

* Отдельные виды `stroke_table`/`stroke_drawing` на `detect` (детектор штриха без surya и без
  исключений) — четыре вида на два понятия, line art внутри фотографий; заменены единым
  детектором с исключениями.
* Блоки surya `Equation` как line art — 19 ложных областей на 200 контрольных без единого
  совпадения с эталоном, и три регрессии `hmean` на эталоне геометрии (дробные черты формул
  внутри рамки рисунка теряют правило текстового штриха); формулы из line art исключены.
* Пересчёт одного семейства без областей других из базы — три «штриха» внутри фотографии
  (1977/01 0040_1L); теперь несчитаемые семейства подаются известными исключениями.
* Дайджест кадра как единственный ключ кэша — попадание требовало бы читать пиксели; ключ —
  отпечаток файла, дайджест только при неизвестном источнике.

## Что дальше

* Решение по `1975_07 с.44` (порча или нет) — от пользователя.
* Кэш surya `fr_geo`/`fr_nogeo` пака-1 набивается фоном (~2 × 1.8 ч); после него —
  `run_scripts/geometry_regression/run_pack1.sh` (v13 по паку) и `run_final_pdfs.sh` на пробном
  выпуске.
* Ложные line art на рамках рубрик — общая слабость `ink`-источника; отдельная задача.
