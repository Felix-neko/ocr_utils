Итак, я экспортировал картинки с закрасом печатей размытием фона (через скрипт-запускатель /home/felix/Projects/ocr_utils/run_scripts/scan_markup/pack1/run_7_cleanup.sh).
Получилась папка `/mnt/SYSTEM/raw/mts/pack1_background_blurred_v2/blurred`

Также я сейчас прогоняю эти сканы через Capture One для усиления деталей текста (это частично полечило проблемы бледных перемычек) и экспортировал такие обработанные сканы в подпапки `sharpened` в каждом выпуске (внутри той же папки blurred).
Прогонка ещё идёт, через несколько часов закончится.

А после того, как я таки экспортирую эти "заострённые" картинки -- я хочу заняться их распознанием в FineReader.

Распознавать их я хочу следующим образом.

Сначала я хочу собрать "заострённые" картинки после Capture One в промежуточные PDF (одна PDFка - это один выпуск). Сборку осуществляем так.
Для каждой страницы выпуска -- берётся "заострённая" картинка после Capture One.
Если на этой странице нету растровых объектов, цветного текста и др -- эту картинку добавляем в PDF как есть, без перекодирования.
Если на этой странице есть растровые объекты, цветной текст и др. (согласно данным в sqlite-базе) -- то на "заострённую" картинку вставляем соответствующий фрагмент из оригинальной картинки до "заострения" (на случай, если я всё-таки захочу наделать DOCX-документов с картинками, чтобы текст там был заострённым, а растровые картинки нет). Желательно обойтись без перекодирования текстовой части нашей страницы.
Сохраняем по каждому выпуску две PDF:
- полную промежуточную PDF со всеми страницами (эту я буду в FineReader пропускать через бинаризацию и через распрямление строк);
- промежуточную PDF типа PAGES_WITH_PICS_ONLY (только страницы, содержащие растры) -- эти я в FineReader буду пропускать через бинаризацию, но без распрямления строк.
Именование PDF:full_{year}_{issue}.PDF и pages_with_pics_only_{year}_{issue}.PDF (issue -- не забываем про ведущие нули, если номер месяца однозначный).

PDFки всего пака будут не раскладываться в папки по годам, а лежать всего в двух папках для целого пака: full_intermediate_pdfs и intermediate_pdfs_pages_with_pics_only

Я сделаю для этих PDF пакетное распознание в FineReader, после чего будут получены две новых папки, где будут лежать PDF уже с текстовым слоем после обработки в FineReader:
- папка для распознанных полных PDF с текстовым слоем (бинаризованы, с распрямлением строк);
- папка для распознанных PDF типа PAGES_WITH_PICS_ONLY с текстовым слоем (но без распрямления строк, чтобы геометрия страницы осталась прежней, и в финальные PDF можно было скопировать color/grayscele растровые картинки из оригинальных изображений в то же место, где они и были, только конвертнув их в JPG и наложив верхним слоем).


После этого мне нужно будет, чтобы ты распознанных PDF собрал финальную PDF для каждого выпуска (используя sqlite-базу):
- если страницы без растра -- копируем из полной бинаризованной PDF как есть;
- если страницы с растром -- копируем такие страницы из распознанной PDF типа PAGES_WITH_PICS_ONLY, но там, где находятся растровые картинки / цветной текст -- копируем верхним слоем эти фрагменты из оригинальных картинок (только конвертируем их в JPEG с качеством 75, и если это картинка типа grayscale -- то в grayscale JPEG)

1) Сделай мне дополнительный python-скрипт (в pdf_utils), который будет забирать сканы из sharpened-подпапок и складывать их в структуру папок типа {год}/{выпуск}/и_тут_уже_картинки)т.е. чтобы структура для новых экспортированных фоток была как в папке `/mnt/dump3/yandex_disk_linux_baby_zergling/Общее/Фотки/МТС/Готовое/пак-1`
и чтобы папки sharpened из папки  `/mnt/SYSTEM/raw/mts/pack1_background_blurred_v2/blurred` в итоге исчезли. И сделай мне скрипт-запускатель для этого python-скрипта.

2) В наших sqlite-базах пора вводить новые сущности.

Для таблицы `packs` -- вместо одного пути root_path будут несколько путей:
- source_pics_root: бывший root_path (т.е. путь к директории с нарезанными сканами до нашей разметки);
- sharpened_text_pics_root: путь к папке с картинками с усиленными деталями текста;
- full_intermediate_pdf_root: путь к папке, куда будем складывать промедуточные PDF
- pages_with_pics_only_intermediate_pdf_root: путь к папке, куда будем складывать PDF типа PAGES_WITH_PICS_ONLY
- final_pdfs_root: путь к папке, куда будем складывать финальные PDF

Для таблицы pages:
- file_name переименовать в source_file_name
- rel_path переименовать в source_rel_path
- добавляем опциональные поля: sharpened_text_pic_file_name и sharpened_text_pic_rel_path (т.е. имя файла и путь к картинке с заострённым текстом, которые картинки я получил через Capture One)
- добавляем опциональное поле: full_pdf_page_idx (номер страницы в полном промежуточном PDF начиная с 0);
- добавляем опциональное поле: pages_with_pics_only_pdf_page_idx (номер страницы в промежуточном PDF типа PICS_ONLY, если она должна попасть в PDF типа PICS_ONLY (т.е. если в ней были размечены какие-то растровые регионы или цветной текст), если её не должно быть в промежуточном PDF типа PICS_ONLY, то тут будет None/NULL)

Для таблицы issues -- нужно добавить:
- опциональное имя полной промежуточной PDF (если уже создана)
- опциональное имя промежуточной PDF типа PAGES_WITH_PICS_ONLY (если документ полностью текстовый, то может быть и None даже после экспорта всех PDF)

Если нужны какие-то дополнительные модификации БД и соответствующих классов -- это нормально.

2) Сделай мне python-скрипт, который будет собирать промежуточные PDF, на которых я буду напускать FineReader для распознания.

На вход у этого скрипта будут подаваться:
- путь к нашей sqlite-базе
- путь к директории с оригинальными картинками
- путь к директории с заострёнными картинками
- путь к директории для промежуточных PDF типа FULL
- путь к директории для промежуточных PDF типа PAGES ONLY

Также сделай мне к нему скрипт-запускатель.

3) И следай мне python-скрипт для сборни финальных PDF.
На вход будут подаваться:
- путь к нашей sqlite-базе
- путь к директории с оригинальными картинками
- путь к директории для промежуточных PDF типа FULL
- путь к директории для промежуточных PDF типа PAGES ONLY
- путь к директории финальных PDF
- ключик: раскладывать ли по годам финальные PDF -- или класть все в одну папку