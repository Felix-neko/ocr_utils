"""Стенд v15 детектора «FineReader ухудшил геометрию»: перекос блоков, заголовки, выигрыш в таблицах.

Ядро (``ocr_utils.geometry_regression``, v14) не трогается: здесь его кирпичи (поле смещений,
строки, штрихи, кромки фото) собраны заново с другими метриками там, где разбор 54 проблемных
страниц пользователя (2026-09-22) показал ошибки измерения или вердикта:

* перекос выключенного блока (строки горизонтальны, кромка ушла на 1.5–5 мм на колонку) — сдвиг
  из якобиана поля смещений (``field_shear``) с подтверждением по кромкам тех же строк (``blocks``);
* наклон заголовка — по проекции строки в обеих версиях (``lines``), а не по центр-линии
  ``line_fit``, которая на акцидентном шрифте скачет и превращала порчу в «выигрыш»;
* наклон штриха в A — по краске вдоль штриха B (``ridge``), без пары LSD в A (дробные черты
  формул, которые LSD в A теряет; линейка, которой LSD в A даёт ложный подъём); трассировщик
  изгиба с ограничением шага (разрыв линии — конец, а не перескок на соседнюю стрелку);
* группы параллельных — только по осям и вне line art (штриховка диаграмм под 45° — не линейки);
* фото — кромки по каждому снимку в рамке, наклон по снимку целиком (трапеция → параллелограмм
  того же уровня не порча), непрощаемым остаётся только изгиб кромки;
* строки внутри таблиц и рисунков — в выигрыш (не в порчу): без них таблица со сдвинутыми
  графами при выправленных строках не могла стать mixed;
* доворот страницы, подтверждённый выпрямлением текста, вычитается из наклона штрихов;
* одиночный штрих (подчёркивание рубрики) — мягкая порча: bad только без выигрыша.

Версия кэша — ``ENGINE_VERSION``; JSON ядра (v14) стенд v15 не читает и не пишет.
"""

# v16 (2026-09-22, второй просмотр пользователя): кромки блоков по краске рядов, порча line art
# (углы, поворот, разброс, изгиб линий рисунка), дробные черты своим семейством, форма заголовков
# относительными мерами.
ENGINE_VERSION = "v16"
