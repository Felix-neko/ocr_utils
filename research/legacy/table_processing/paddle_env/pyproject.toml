[project]
name = "table-paddle-env"
version = "0.1.0"
description = "Отдельное окружение под PaddleOCR: в основной venv он не ставится"
requires-python = ">=3.11,<3.12"
dependencies = ["paddleocr>=3.0", "paddlepaddle>=3.0", "opencv-python-headless>=4.8", "numpy<3"]

[tool.uv]
prerelease = "allow"
