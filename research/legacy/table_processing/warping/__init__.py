"""Геометрия таблицы: измерить кривизну и выпрямить.

Пакет назван ``warping``, а не ``geometry``, потому что модуль ``geometry`` в этом же
пакете уже занят рамками и сеткой (``Box``, ``Cell``, ``Grid``), и одноимённый подпакет
его бы заслонил.

ЗАЧЕМ. FineReader пропускает таблицу, если её линейки не прямые: либо полоса поехала при
съёмке, либо коррекция геометрии самого FineReader сделала хуже. Замер по паку (команда
``find-broken``): из 132 находок на страницах без таблицы в DOCX 99 — настоящие таблицы,
которые распознаватель не увидел.

ЧТО ЗДЕСЬ ЕСТЬ. ``trace`` прослеживает линейку как ЛОМАНУЮ (в проекте такого не было:
детектор таблиц знает только габарит отрезка), ``metrics`` меряет по ломаным кривизну и
регулярность решётки, ``diagnose`` сравнивает одну таблицу в трёх источниках и говорит, кто
её искорёжил, ``engines`` выпрямляют, ``evaluate`` сравнивает выпрямители между собой.
"""

GEOMETRY_VERSION = 1
